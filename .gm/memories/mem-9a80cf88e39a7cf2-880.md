---
key: mem-9a80cf88e39a7cf2-880
ns: default
created: 1782327417891
updated: 1782327417891
---

gm-method: (1) codesearch index rebuild on a digest-mismatch can take 170s+ (busy_until advertised); the retention sweep deletes out/ response files older than 1h, so a codesearch whose rebuild outran the sweep window leaves NO out file — re-dispatch rather than wait, or read source directly. (2) The browser verb body runs in the Node executor context, NOT the page: DOM globals (getComputedStyle, document) only exist inside page.evaluate(() => ...). A top-level getComputedStyle throws 'not defined'. (3) A consumer demo dist/index.html that loads CSS/JS from unpkg @latest will NOT reflect local src edits — build a local _witness.html loading ./247420.css and serve it (python http.server) to browser-witness reinvention work against the real local build. Live contrast measurement (WCAG ratio in page.evaluate) is the witness for an accent/palette swap, not eyeballing.
