---
key: mem-3cb1ea785cb78cf6-630
ns: default
created: 1783312336737
updated: 1783312336737
---

gm-method: when a browser witness shows a stale/unexpected document.fonts registry or other page-global state that contradicts a fresh fetch() of the same resource, suspect same-tab state accumulation across many prior browser dispatches in one long-lived session, not a real regression. Confirm by opening a genuinely fresh page via page.context().newPage() and re-checking the same assertion in an isolated context before treating the discrepancy as a bug -- Chrome's FontFace registry (and similar in-page caches) can persist stale entries across same-tab navigations even with Network.clearBrowserCache and cache-disabled set.
