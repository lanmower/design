---
key: mem-d636a704acd72e6d-2242
ns: default
created: 1785339065364
updated: 1785339065364
---

{"SESSION_ID":"73be7501-ad32-4d34-bd8e-2cd2d6311c37","memo":"Disarming a per-push force-push workflow across many repos: three non-obvious traps, all hit live while clearing auto-declaudeify.yml from 13 AnEntrypoint repos. (1) DUPLICATE CLONES SHARE A REMOTE. C:/dev/420sched origin is AnEntrypoint/showpick.git and C:/dev/zel origin is AnEntrypoint/zellous.git -- neither directory name matches its repo. Both remote HEADs sat at the same sha as their twin. Pushing from both would double-push one remote; instead push once and verify BOTH working copies report the same originSha, then git checkout -- the redundant local edit so the second tree goes clean. Always read git remote get-url origin per directory; never infer the repo from the folder name. (2) AN ARCHIVED REPO CANNOT BE FIXED AND DOES NOT NEED TO BE. trends-cli push failed 403 remote: This repository was archived so it is read-only. gh api showed archived:true with pushed_at frozen 3 months earlier and its last workflow run the same day. GitHub does not run Actions on an archived repo and a contents:write token cannot push to it, so the force-push path is doubly inert -- correct disposition is blockedBy:[external] with the local fix committed and waiting, NOT a completed status (origin still carries the defect) and NOT a workaround. (3) VERIFY THE REPLACEMENT PARSED, not just that bytes changed. Two behavioural witnesses beat a file read: gh run list per repo showed the newest auto-declaudeify run predating every fix sha (so no push-triggered run fired -- GitHub evaluates the workflow file AT the pushed ref, so the new workflow_dispatch-only file governed its own arrival), and gh api repos/<slug>/actions/workflows showed state:active for the replacement in all 7 pushed repos, proving valid YAML rather than silent rejection. Damage-signature note: the version-behind-npm probe found ZERO drift across all 13, but the tag probe (git rev-list -n 1 <tag> then git branch -r --contains <sha>) found real orphans -- webrig 34 of 35 tags, thorns 4 of 4 with origin at 5.1.9 vs last tag 2.2.0, agent-remote 3 of 6. Repeated shas across adjacent tags (webrig v1.0.20/v1.0.21) are the re-tag-after-rewrite tell. Run BOTH probes; the tag one is far more sensitive."}
