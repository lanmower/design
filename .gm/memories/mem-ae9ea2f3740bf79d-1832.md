---
key: mem-ae9ea2f3740bf79d-1832
ns: default
created: 1785337488511
updated: 1785337488511
---

{"memo":"A green publish workflow proves nothing; read STEP conclusions, not the job's. Two silent-failure mechanisms in AnEntrypoint/spoint and AnEntrypoint/agentgui, both reporting success while npm never moved. (1) spoint publish.yml computed the next version from local package.json alone (standard-version patch+1); with local 0.1.463 behind the registry's 0.1.464 the computed tag already existed, so the bump step set skip=true and exit 0 and the guarded Install+Publish steps were SKIPPED while the job went green -- run 30454309245: job publish=success, step 'Publish to npm'=skipped. Fix: base the bump on max(registry,local) then patch and delete the skip branch so no path goes green without publishing. (2) The deeper one, in BOTH repos: auto-declaudeify.yml triggered on push branches ['**'], ran git filter-branch --force -- --all then git push --all --force from a checkout predating the concurrent publish job's bump commit, racing it on the same sha and destroying it. agentgui run 30462473770 logged '096f766..ce4a898 HEAD -> main' and 'Successfully pushed version bump' yet refs/heads/main stayed 096f766 and ce4a898 became unfetchable; spoint tag v0.1.465 likewise pointed at a commit absent from main. Its author check could never stop firing (git log --all --author=Claude matches 69 historical commits forever) so the force-push was unconditional and permanent -- why main drifted 13 versions behind npm while every publish 'succeeded'. Lesson: an automated per-push force-push of rewritten history silently eats any commit a concurrent job pushes; history rewriting must be manual and exclusive. Verified: spoint origin/main 0.1.466 == npm 0.1.466, agentgui origin/main 1.0.1114 == npm 1.0.1114. NPM_TOKEN present and valid in both -- no missing secret.","SESSION_ID":"73be7501-ad32-4d34-bd8e-2cd2d6311c37"}
