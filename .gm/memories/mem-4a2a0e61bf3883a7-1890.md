---
key: mem-4a2a0e61bf3883a7-1890
ns: default
created: 1783328786640
updated: 1783328786640
---

gm-method: when a PRD dedup-sweep row asks to verify N sibling repos have no private reimplementation of a shared library primitive (e.g. a hash-router class), the correct witness sequence is (1) Read the shared primitive's own source for its exact API surface (method names: parseHash/register/navigate/render/handlePopState/start), (2) Bash find across every sibling for filenames matching the primitive's domain (e.g. *router*), excluding node_modules/.git, (3) for each hit, Read it and compare API shape -- a same-named file in a build-OUTPUT directory (dist/docs) with a DIFFERENT API shape (e.g. upstream webjsx's own bundled pathname router: initRouter/goto/match vs the SDK's hash-based Router: parseHash/register/navigate) is dead vendor spillover, not an authored duplicate; confirm via grep that no script/import in that output tree references the file at all, (4) grep each sibling's actual authored source (theme.mjs etc, not build output) for the primitive's signature calls (location.hash, class Router, popstate, hashchange, createRouter) -- a single incidental location.hash read (e.g. extracting query params appended after '#') with no route table/dispatch/listener is not a router reimplementation. Separately: PRD/mutable resolve verbs in a shared multi-session gm watcher can resolve a DIFFERENT id than the one passed in the request body (observed twice consecutively resolving unrelated queue-head rows instead of the targeted id) -- this reflects queue-order dispatch under concurrent-session contention on the same .gm/exec-spool, not a targeting bug in the calling agent; recovery is to re-dispatch instruction, confirm via the fresh prd_items/mutables_pending list whether the intended row is now absent/resolved, and re-issue prd-resolve/mutable-resolve with the same id if it reappears pending, rather than assuming the first response's returned id was wrong.
