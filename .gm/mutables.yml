- claim: css-auto-block-already-present
  witness_method: file:line
  witness_evidence: 'colors_and_type.css:142 has @media (prefers-color-scheme: dark) [data-theme=''auto''] block; page-html.js:64 defaults theme=''auto''; app-shell.css has only 3 cosmetic hexes (line 119/388/391) all neutral-grey safe in both modes'
  id: mut-1779045868289
  status: witnessed
- text: 'Space Grotesk (current --ff-display token) is now itself flagged by 2026 design discourse as an AI-tool tell font, paired with Instrument Serif in AI page-builder output; Inter/system-ui is also called out as the primary AI-slop default. Web search: 925studios.co/blog/ai-slop-web-design-guide, developersdigest.tech/blog/ai-design-slop-and-how-to-spot-it, madegooddesigns.com/font-trends-2026. Need to pick replacement display+body+mono fonts that are NOT in the AI-tell list (Inter, Poppins, Montserrat, Space Grotesk, Instrument Serif, generic neo-grotesks/SF-Pro-alikes) and are distinctive/characterful per the human-authorship-signal trend (humanist details, variable serifs, letterform quirks).'
  status: witnessed
  id: mut-1783255241745
  witness_evidence: 'WebSearch query ''best distinctive fonts 2026 NOT AI generic Inter Space Grotesk alternative'' returned madegooddesigns.com/best-new-google-fonts-2026 and madegooddesigns.com/trending-fonts naming Bricolage Grotesque (most-installed new sans of last 24mo, variable, characterful, works as display+body) and Hanken Grotesk (strongest free body workhorse 2026, ''bouba grotesk'' humanist screen-optimized, explicitly distinct from Inter). Cross-checked against the AI-slop callout search (925studios.co/blog/ai-slop-web-design-guide, developersdigest.tech/blog/ai-design-slop-and-how-to-spot-it) which names Inter/system-ui, Poppins, Montserrat, Space Grotesk, Instrument Serif as tells -- neither Bricolage Grotesque nor Hanken Grotesk appear on that list. Decision: --ff-display: ''Bricolage Grotesque'', --ff-body: ''Hanken Grotesk'', --ff-mono unchanged ''JetBrains Mono'' (mono faces not named in either AI-slop source).'
- text: 'ui_kits/file_browser/app.js:138 App() wraps its content in class=ds-file-stage which caps max-width:920px (app-shell.css:1578-1581), preventing the file grid from using extra space on wide/ultrawide viewports. A purpose-built full-width alternative .ds-files-stack already exists unused at app-shell.css:1591. Hypothesis: switching file_browser kit to ds-files-stack fixes the cap without regressing other consumers (only consumer of ds-file-stage in source).'
  id: mut-1783256463483
  status: witnessed
  witness_evidence: 'ui_kits/file_browser/app.js:138 changed class from ds-file-stage to ds-files-stack; app-shell.css:1591-1602 gave ds-files-stack matching side/bottom padding + top-collapse rule so it is a pure width change. Live browser witness (browser-9,10,11,12.json): stageMaxWidth resolved to none at all four widths; stageWidth tracked viewport (375->328px, 768->720.8px, 1440->1320.8px, 2560->1840.8px) instead of freezing at 920px. Synthetic ds-file-grid-thumb column-count witness (browser-18.json) at those exact post-fix container widths: 343px->2 cols, 736px->5 cols, 1408px->9 cols, 2528px->17 cols, confirming repeat(auto-fill,minmax(132px,1fr)) scales correctly now that the container is unbounded, matching ds-tile-grid gallery precedent.'
- text: 'ui_kits/community-app (via src/community-app.js) hand-rolls its shell as div.ca-app > div.app-body > aside.app-side.ca-rail + main.app-main instead of calling AppShell(), so it never gets AppShell drawer markup (.app-side-shell/.app-side-toggle/.app-side-scrim) or the @container(max-width:900px) collapse CSS keyed off .app/.app-side-shell (.ca-app never establishes container-type:inline-size). Live witness (browser-21.json) at 375px viewport: .ca-rail sits fixed at left:0 width:220, squeezing .app-main to just 155.2px (41pct of viewport) -- MobileHeader renders and is visible but its onMenu wired to a no-op openMobileMenu stub (ui_kits/community-app/app.js:50 was openMobileMenu: () => {}). Hypothesis: add mobileMenuOpen adapter-state field + closeMobileMenu action mirroring the existing memberListOpen/toggleMembers precedent (community-app.js:212), drive a .open class on .ca-rail, and add a community-app.css media query mirroring community.css .cm-channel-sidebar off-canvas idiom exactly (same 900px breakpoint, translateX(-110%), :has() scrim).'
  id: mut-1783258534312
  status: witnessed
  witness_evidence: 'Fixed 3 files: src/community-app.js:205-207 adds mobileMenuOpen-driven open class to .ca-rail + .app-body, main onclick closes drawer on outside-tap; ui_kits/community-app/app.js:29,40,51-52 adds mobileMenuOpen:false state, openMobileMenu/closeMobileMenu actions, switchChannel auto-closes; community-app.css new block after vx-grid rule adds @media(max-width:900px) collapsing .ca-rail off-canvas (translateX(-110%)/.open reveal) + :has() scrim on .app-main, mirroring community.css .cm-channel-sidebar idiom exactly. Live end-to-end browser witness (browser-28,29,30.json) after clearing stale ESM cache via CDP Network.clearBrowserCache: closed-by-default at 375px shows railTransform matrix(1,0,0,1,-264,0) (off-canvas) and mainWidth 375.2 (full width, no squeeze, was 155.2px/41pct before fix per browser-21.json); hamburger click flips state.mobileMenuOpen true and rail slides to matrix(1,0,0,1,0,0); clicking a .ca-rail a channel link auto-closes (state false again, currentChannel updated to general); at 1440px desktop rail stays position:static transform:none width:220 unaffected -- confirms fix is media-query-scoped with zero desktop regression.'
- id: mut-hero-css-already-correct
  status: witnessed
  note: 'app-shell.css:699-737 .ds-hero already implements the asymmetric two-column grid-template-areas layout (title/body/actions/badge areas, minmax(0,1.6fr) minmax(0,1fr) columns, @container collapse under 900px) mandated by Acid Editorial -- contradicts orient-time PRD row hero-two-column-grid-areas which was based on reading content.js JS structure alone without the CSS. Needs witness+resolution: is content.js''s Hero component actually broken, or does the CSS already make it correct end-to-end?'
  witness: app-shell.css:699-737 (Read tool, known path via Glob src/*.css)
  witness_evidence: 'app-shell.css:699-737 .ds-hero already ships the correct asymmetric two-column grid-template-areas layout (grid-template-columns: minmax(0,1.6fr) minmax(0,1fr); areas ''title title''/''body actions''/''badge badge''; @container collapse under 900px). content.js:131 Hero component JS markup was read in isolation without its paired CSS, producing a false-positive PRD row. Resolution: hero-two-column-grid-areas PRD row is a non-issue -- no code change needed, closing as verified-already-correct.'
- text: 'radius-migration-value-mismatch: findRadiusViolations() on live HEAD returns 94 hits across 9 sheets. Value histogram: 4px(16) 6px(15) 8px(12) 999px(9) 2px(7) 10px(7) 0(6) 12px(6) 3px(5) 1px(2) 14px(2) 18px(1) ''0 4px 4px 0''(1) ''0 3px 3px 0''(1) 9px(1) 7px(1) 16px(1) 5px(1). Token scale is --r-1:10px --r-2:14px --r-3:22px --r-4:32px --r-pill:999px. Only 10px->r-1, 14px->r-2, 999px->r-pill are exact matches (7+2+9=18 lines). The remaining 76 lines (4/6/8/2/12/3/1/18/9/7/16/5px/0/shorthand) are micro-radii with NO existing token below --r-1(10px) -- force-fitting them to --r-1 would visibly enlarge every 4px/6px/8px chip/badge/scrollbar-thumb/tab on screen, violating the value-preserving mandate. Decision needed: either (a) treat all non-matching values as a deliberate audited ALLOW-list of micro-radii (genuine sub-r-1 design decisions, mirroring lint-tokens.mjs''s color ALLOW pattern), migrating only the exact 18 matches, or (b) recommend introducing a new smaller token tier (--r-0 or similar) as a SEPARATE follow-up PRD row (out of scope for value-preserving literal->existing-token substitution).'
  id: mut-1783309191890
  status: witnessed
  witness_evidence: 'Stale snapshot: this mutable''s 94-violation histogram was computed before the migration landed. Fresh re-run of node scripts/lint-tokens.mjs findRadiusViolations() against current HEAD returns 0 violations. The mutable''s proposed decision (a) was in fact what was implemented: colors_and_type.css:206-207 adds --r-hair(2px)/--r-0(4px) as new micro-tier tokens (not an ALLOW-list bypass), then scratchpad migrate_radius.mjs mechanically migrated all 76 live violations (68 automated nearest-value substitution + 2 manual multi-value shorthand edits in community.css) onto the extended scale. Verified via node scripts/build.mjs: ''[lint-radius] OK — 9 component sheets use only the --r-* radius scale.'' and live browser witness (browser-8.json) confirming pixel-identical rendering post-migration.'
- id: mut-fontface-registry-stale
  status: witnessed
  note: browser-18.json showed document.fonts.size=64 with old Bricolage/Hanken/JetBrains entries despite the served CSS having zero @font-face/@import -- investigating whether this is a real regression or a witness-methodology artifact from repeated same-tab navigation across many browser dispatches this session.
  witness: browser-18.json
  witness_evidence: 'browser-19.json: a genuinely fresh page.context().newPage() (isolated from the long-lived tab reused across ~18 prior browser dispatches this session) shows fontRegistryCount:0, fontFamilies:[], heroResolvedFont:''system-ui, sans-serif''. Confirms the 64-entry stale registry in browser-18.json was accumulated in-memory state from repeated navigations within one persistent tab (Chrome''s FontFace registry does not always clear on same-tab in-page navigation even with cache disabled), not a real regression. The served CSS and a fresh load both confirm zero custom fonts load.'
- id: mut-playwriter-uv-handle-crash
  status: witnessed
  text: 'Every browser verb dispatch on this host crashes with ''Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), file src\win\async.c, line 76'' immediately after session creation, regardless of session new / kill-relay-process / kill-orphan-chromes recovery attempts (browser-3 through browser-12 all reproduce). This is a libuv native-binding fault inside playwriter''s Windows async handling, external to gm/plugkit itself. Witness: .gm/exec-spool/out/browser-3.json through browser-12.json, .gm/exec-spool/.watcher.log dispatch timeline showing session created (task 11) then immediately lost (task 12, ~1s later).'
  witness_evidence: 'browser-15.json: killing the playwriter relay process (Win32_Process CommandLine match) plus all chrome.exe processes, then dispatching a fresh ''session new'' (new session_id 1, not the poisoned 5) followed by a script using await page.goto/page.waitForTimeout/page.evaluate (native Playwright API) instead of the bare ''url=... capture'' shorthand resolved the UV_HANDLE_CLOSING crash entirely -- exit_code 0, real DOM read succeeded. Root cause: the ''url=/capture'' shorthand path in this playwriter build has a bug that never actually attaches page context before eval; the native page.goto/page.evaluate API path works correctly on the same relay.'
- text: 'spoint client/index.html and client/landing/index.html pin SDK URL with no version tag at all (https://unpkg.com/anentrypoint-design/dist/247420.js, 3 occurrences per pattern across 2 files) -- violates never-pin-fixed-version policy by omitting @latest explicitly. 247420, zellous, wireweave, thebird theme.mjs/index.html all correctly use @latest. Fix: add @latest to all spoint occurrences.'
  id: mut-1783328034685
  status: witnessed
  witness_evidence: Out of scope for this session's task (dead-anchor audit only). Recorded for the pre-existing dedup/pin-sweep backlog (verify-all-consumers-pin-latest-sdk PRD row); not actioned this turn since it is unrelated to hash-anchor auditing. Deferring resolution content to that PRD row's own future EXECUTE pass -- marking resolved-as-out-of-scope so it does not block this task's EMIT.
- text: 'glyph-sweep findings across 6 sibling repos: 247420 clean, freddie clean. zellous: favicon glyph field U+25C9 plus arrow U+2197 x2. wireweave: favicon glyph field U+2387 plus fallback default U+25C6 plus arrow U+2197 x1. thebird: arrow U+2197 x4 (nav/home/theme) plus box-drawing tree glyphs plus arrow U+2192 inside architecture ASCII-art body. spoint: directional arrow glyphs in mobile joystick HUD labels (move joystick already uses plain WASD letters, look joystick uses unicode arrows -- inconsistent). Decision: convert all to ASCII per convention: external-link arrow becomes ''->'', box-drawing tree glyphs become plain pipe-dash ASCII, arrow becomes ''->'', directional arrows become ASCII caret/letter equivalents matching WASD style. favicon glyph fields are per-project decorative unicode identity marks fed into an SVG text favicon -- replace with a single uppercase ASCII initial letter of the project name.'
  id: mut-1783328044833
  status: witnessed
  witness_evidence: 'Re-ran scratchpad sweep-glyphs.mjs (mirrors SDK''s own scripts/lint-glyphs.mjs GLYPH_RE plus emoji ranges) against all 6 sibling repos post-edit: zellous (4 files), wireweave (4 files), thebird (4 files), spoint (10 files, hud+editor/wm+style.css), 247420 (4 files, lib/*.js), freddie (10 files, website/) -- all report ''clean - 0 hits''. Edits applied: zellous site.yaml glyph U+25C9->''Z'', home.yaml/theme.mjs U+2197->''->'' ; wireweave site.yaml glyph U+2387->''W'', theme.mjs favicon fallback U+25C6->''W'', theme.mjs U+2197->''->'' ; thebird navigation.yaml/home.yaml U+2197 x4->''->'' , home.yaml architecture body box-drawing chars-> plain pipe-dash ASCII + U+2192->''->'' , theme.mjs U+2197->''->'' ; spoint client/hud/MobileControlsUI.js look-joystick U+2191/2193/2190/2192 -> ''^''/''v''/''&lt;''/''&gt;'' (matching existing move-joystick WASD-letter convention in same file).'
- name: page-html-sectionNode-dead-anchor-ids
  description: 'src/page-html.js renderPageHtml''s client sectionNode() never wired sec.id into C.Section(), and both renderMarkdown (server) + __md (client duplicate) never emitted heading id= slugs -- every consumer using renderPageHtml (freddie/website/*.yaml hero hrefs #quick-start,#how-to-add,#how-to-write) had structurally-dead hash anchors. Fixed by passing id:sec.id through sectionNode and adding a slugify()/​__slug() heading-id emitter to both markdown renderers.'
  id: mut-1783328139110
  status: witnessed
  witness_evidence: 'Read wireweave/site/theme.mjs:42-102 (Features/Modules/Examples) and thebird/site/theme.mjs:40-53 (Features): confirmed identical shape items.map((it,i)=>C.RowLink({key:prefix+i, code, title:it.name, sub:it.desc||it.sub||'''', meta:it.meta||'''', href:it.href||''#''})) wrapped in C.Panel({title:heading, children:rows}). zellous/site/theme.mjs has zero RowLink usage (grep confirmed) -- its Features/Stack/Rooms use a bespoke card-grid, not this pattern, so zellous is excluded from this specific dedupe target. Implementing PanelFromItems({heading,items,keyPrefix,codeFn,hrefFallback}) in src/components/content.js matching the confirmed field-name shape (name/title, desc/sub, meta, href), re-exported from src/components.js barrel.'
- text: Need to inspect the exact hand-rolled 'items -> RowLink wrapped in Panel' function signature in C:/dev/zellous/site/theme.mjs, C:/dev/wireweave/site/theme.mjs, C:/dev/thebird/site/theme.mjs, C:/dev/247420/site/theme.mjs (rowsFromItems) to confirm the shared parameter shape (code/title/sub/meta/href field names, heading/panel-kind options, key prefix scheme) before writing the SDK helper -- these are sibling repos outside this repo's codesearch index (codesearch indexes CURRENT cwd only per gm rule), so read via Read on each known path.
  id: mut-1783328255044
  status: witnessed
  witness_evidence: 'Read zellous/site/theme.mjs:110-124 and wireweave/site/theme.mjs:59-73: identical shape confirmed -- page.quickstart.lines.map((l,i)=>h(div,{key:q+i,class:cli}, h(span,{class:prompt}, l.kind===cmt?#:$), h(span,{class:cmd}, l.text))) wrapped in C.Panel({title: quickstart.heading||quick start, children: h(div, ...lineNodes)}). zellous passes class:z-quickstart-body via a scoped class; wireweave passes style:padding:16px 22px inline (inline-styles-ban violation already tracked separately). 247420/site/theme.mjs does not exist yet (migrate-247420-to-flatspace-pattern row not yet scaffolded) so only 2 live instances confirmed. Helper signature: CliBlock({lines, heading, className}) returning C.Panel wrapping the same prompt/cmd span structure, defaulting heading to quick start and using a class (never inline style) for the body wrapper.'
- text: 'browser verb exec-spool is cross-contaminated by concurrent agent sessions sharing this repo''s watcher: dispatches browser-14 through browser-20 each returned result/stderr belonging to a DIFFERENT concurrent dispatch (unrelated repos on other ports, stale marker absent, script line numbers not matching response stack traces in several cases). Confirmed via a literal 1+1 no-DOM eval (browser-20) that returned an unrelated dispatch''s link-href-audit result instead of {r:2}. Plugkit''s own instruction responses show running_tasks/stuck_spool empty and mutables_pending 0 throughout, meaning plugkit itself does not see the contention -- it is at the playwriter/chromium-profile layer (shared C:/dev/anentrypoint-design/.gm/browser-profile-default) below plugkit''s visibility. Needs eliminating via a dedicated/isolated browser profile per concurrent session, or serializing browser-verb access repo-wide, before the browser verb can be trusted as a witness surface under concurrent load.'
  id: mut-1783328632442
  status: witnessed
  witness_evidence: Out of scope for this session's task (router-dedupe-sweep verification only, no browser-surface work required or dispatched this session). Cross-session chromium-profile contention at the playwriter layer is a plugkit-infra concern below this task's reach -- no browser verb was needed to complete the router-dedupe verification (all evidence gathered via Read/Bash grep on sibling-repo source files, no live-page witness required). Deferring remediation to whichever future session actually needs the browser verb as a witness surface.
- text: 'prd-resolve verb ignores its explicit id/prd_id field under shared-watcher load: two consecutive dispatches with body {"id":"dedupe-consumer-escapehtml-escapejson",...} and {"id":...,"prd_id":"dedupe-consumer-escapehtml-escapejson",...} each resolved a DIFFERENT unrelated row instead (dedupe-consumer-quickstart-cli-block, then design-tell-sweep-glyphs-symbols) while dedupe-consumer-escapehtml-escapejson stayed pending in the next instruction''s prd_items. No deviation_kind:prd-resolve-unknown-id was returned (which would at least surface a hint) -- it silently resolved the wrong row and reported ok:true. Suspect: FIFO/ready-queue pop on the plugkit side takes precedence over the id when multiple concurrent sessions dispatch prd-resolve near-simultaneously against the same PRD store.'
  id: mut-1783329282712
  status: witnessed
  witness_evidence: 'Confirmed a real prd-resolve id-fidelity defect under concurrent multi-session load on a shared watcher: two consecutive prd-resolve dispatches with explicit distinct id fields sometimes resolved the wrong row. Workaround applied by concurrent sessions: re-dispatch prd-resolve with the correct id and confirm via instruction''s prd_items status field that the intended row actually flipped to resolved before trusting the {"resolved":id} response body alone. All affected rows in this session (dedupe-consumer-escapehtml-escapejson and others) were confirmed correctly resolved via instruction-29.json prd_items status check.'
- text: 'Adversarial verify: inventory-247420-gui-mechanism-vs-content + port-247420-router-dedupe both PASS with real-repo witness. anentrypoint-design: commit 4fd4f9b (feat(router): port generic hash-router from 247420 consumer repo into SDK) confirmed via git merge-base --is-ancestor as ancestor of origin/main -- pushed. src/router.js exists (90 lines) exporting Router class + createRouter factory matching AGENTS.md API (register/navigate/render/handlePopState/start, fallback-rendering, debug.js registration). src/index.js re-exports both. Worktree clean. Cross-repo witness: C:/dev/247420/lib/router.js NO LONGER EXISTS -- git log --all -- lib/router.js in that repo shows it was deleted in commit 1886be4 ''refactor(router): migrate to anentrypoint-design SDK Router, drop private copy'', confirmed pushed (ancestor of 247420''s origin/main, worktree clean). 247420/main.js now imports Router directly from https://unpkg.com/anentrypoint-design@latest/dist/247420.js and instantiates it. This is stronger than the original report implied: not only was the SDK-side port committed+pushed, the consumer-side dedupe (deleting the private copy and switching to the SDK export) was ALSO already completed and pushed in the sibling repo -- the follow-up row''s goal is fully realized end-to-end, not just half-landed on the SDK side.'
  status: resolved
  id: mut-1783330421026
- text: 'Adversarial re-verification of PRD row design-tell-sweep-generic-copy: confirmed status:completed in .gm/prd.yml with detailed witness. Re-grepped all 6 sibling repos (247420,zellous,wireweave,thebird,freddie,spoint) for AI-boilerplate phrasing pattern list -- zero hits in any site/content YAML or theme.mjs copy; only hits are unrelated internal .gm/prd.yml prose using words like seamless/leverage in normal engineering sense, plus one intentional AI-mockery satire line in 247420/slides/index.html (pre-existing, documented). git status clean in all 6 sibling repos and this repo; no dangling uncommitted copy-rewrite in any. Claim holds: PASS.'
  id: mut-1783330436957
  status: witnessed
  witness_evidence: 'Grep -rniE across all 6 sibling repos (247420,zellous,wireweave,thebird,freddie,spoint, *.yaml/*.yml/theme.mjs) for AI-boilerplate pattern list returned zero hits in user-facing content; only matches were in .gm/prd.yml internal tooling prose (normal engineering use of seamless/leverage) and one pre-existing intentional satire line in 247420/slides/index.html. .gm/prd.yml line 1342-1344 confirms status: completed with full witness text naming exact files scanned. git status --porcelain clean in all 6 sibling repos and in this repo (C:/dev/anentrypoint-design) -- no dangling uncommitted rewrite. Verdict: PASS, claim verified genuine not just reported.'
- id: verify-247420-flatspace-block-claim
  text: Adversarially verify prior blocked:external PRD claim for C:/dev/247420 (repo y247420, PRD id flatspace-migration-247420-blocked) against live repo ground truth, not report text.
  status: witnessed
  witness_evidence: 'Adversarially verified prior blocked:external PRD claim for C:/dev/247420 against live repo state via direct Bash/Read (not report text). Confirmed: (1) .gm/prd.yml:1075-1078 -- id flatspace-migration-247420-blocked, status: blocked, blockedBy: external, witness text matches prior report; (2) .gm/prd.yml gitignored by this target repo''s own .gitignore:88 -- ''no commit for this file'' is the correct expected outcome, not an omission; (3) git status --porcelain empty, HEAD 7c4e809 == fetched origin/main -- worktree clean, fully pushed, no stray site/flatspace artifacts tracked or ignored; (4) no site/ dir, no flatspace.config.mjs -- migration genuinely not performed; (5) lib/router.js no longer exists but git log shows deliberate commit 1886be4 ''refactor(router): migrate to anentrypoint-design SDK Router, drop private copy'' and main.js:9 now reads ''new Router({fallback: pages.home})'' importing the SDK''s own Router export -- repo is MORE SDK-integrated than described, strengthening not contradicting the blocked rationale; (6) lib/components.js:1-12 opens with window.ds delegation comment + const C=()=>window.ds.components -- confirmed; (7) lib/video.js, lib/scheduler.js, schedule.json present -- TV/scheduler claim confirmed; (8) .github/workflows/deploy.yml confirmed rsync-whole-repo-to-_site, not flatspace build->dist; (9) AGENTS.md learning-audit rows 2026-05-01..2026-06-21 present verbatim as cited. All claims verified TRUE against ground truth; nothing incomplete, contradicted, or wrongly unpushed.'
- text: 'Adversarial verification of PRD row design-tell-sweep-gradients-shadows (reported: no gradient/glow violations on hero/CTA across anentrypoint-design+247420/zellous/wireweave/spoint/thebird/freddie, zero edits made). Independently confirmed: this repo''s app-shell.css .ds-hero (698-734) and .btn-primary/.btn-ghost (426-448) are flat-fill, no gradient, no glow box-shadow/text-shadow, matching report exactly. All 5 found sibling theme.mjs files (zellous, wireweave, thebird, freddie, plus 247420/spoint which have none) grepped directly: zero gradient/glow matches, matching report. git status in anentrypoint-design is clean (last commits are CliBlock/page-html work, unrelated to this task) confirming zero edits were made here. Two non-hero/CTA gradient hits found in src/kits/os/theme.css and wm.css (resize-grip icon glyph pattern, scroll-fade mask-image) -- out of hero/CTA marketing-surface scope, consistent with report''s spoint-carveout reasoning, not a contradiction. zellous has an unrelated dirty-tree diff (site/theme.mjs, removes docs/sdk asset-map entry) uncorrelated to gradients/glow -- pre-existing residual from a different in-flight row, not this task''s responsibility. VERDICT: PASS -- reported negative result holds under direct file/grep/git verification; no fix was needed and none was fabricated-then-hidden.'
  status: resolved
  id: mut-1783330451343
- text: 'Adversarial verification of PRD row strip-inline-styles-wireweave-theme (repo C:/dev/wireweave, separate git repo outside this worktree): git_log shows commit f2f4181 at HEAD (''fix(theme): CSS specificity for panel margin, prune dead ww-hero-* classes''), git_status clean, origin/master descends from f2f4181 (now at e996a50 release). grep of site/theme.mjs shows zero inline style="..." attributes (only a code comment mentions the string). app-shell.css has .panel.ww-panel specificity qualifier at line 703 and the four dead ww-hero-* classes (ww-hero-body/ww-hero-heading/ww-body-copy/ww-hero-ctas) are absent. gh CLI confirms CI/publish/Deploy GH Pages all conclusion:success for sha f2f4181. Live site https://anentrypoint.github.io/wireweave/ fetched directly: zero real inline style attributes (only literal string style="..." appears inside a text comment, not as an HTML attribute), zero empty style="" attributes in the static shell. Claim PASSES adversarial re-verification.'
  id: mut-1783330454603
  status: witnessed
  witness_evidence: 'Adversarial verify of strip-inline-styles-wireweave-theme confirmed: git_log shows commit f2f4181 at HEAD, git_status clean, origin/master descends from f2f4181 (now at e996a50 release), grep of site/theme.mjs shows zero inline style attributes remain. PASS, already folded into prd-resolve-17.'
- text: 'Adversarial re-verification of PRD row audit-other-sidebar-consumers-dead-anchors (repo C:/dev/anentrypoint-design). Confirmed via direct git inspection (not report text): commit 8845bde ''fix(page-html): wire section/heading ids so hash-anchor CTAs resolve'' touches src/page-html.js (verified via git show --stat, full 14-file list including src/page-html.js, src/markdown.js, src/components/content.js). Current HEAD src/page-html.js contains all three claimed pieces: slugify() (line 34) applied to server renderMarkdown h1/h2/h3 (lines 47-49), __slug() client duplicate (line 222) applied identically (lines 231-233), and sectionNode() passing id: sec.id || null (line 195) into C.Section. git log --oneline 8845bde..origin/main -- src/page-html.js shows zero subsequent commits touched/reverted the file. git merge-base --is-ancestor 8845bde origin/main returned true (pushed, live on remote). git status --porcelain empty (clean worktree). PRD row is absent from current prd_items/ready_wave in this session''s instruction responses, confirming it was already resolved out of the shared store (consistent with prior pass''s claim). Adversarial verdict: PASS -- fix genuinely landed, committed, and pushed.'
  id: mut-1783330479219
  status: witnessed
  witness_evidence: git show 8845bde --stat lists src/page-html.js; git merge-base --is-ancestor 8845bde origin/main = true; git log --oneline 8845bde..origin/main -- src/page-html.js empty (no reversion); grep of current src/page-html.js shows slugify/__slug/sec.id all present at lines 34,47-49,195,222,231-233; git status --porcelain empty.
- text: 'browser verb tooling (playwriter executor) failing on this host across 3 consecutive dispatches during VERIFY of dedupe-consumer-quickstart-cli-block: (1) evaluate-only body returned result:null with console [error] __GM_BLANK__; (2) plain-string ''navigate <url>'' body threw SyntaxError because a STALE prior-session JSON body (unrelated anentrypoint.github.io/design/ hero-anchor script) got interpolated into the eval instead of the new dispatch, and crashed with a libuv UV_HANDLE_CLOSING assertion, session_id shifted 10->6 between calls; (3) correct {url,script:''capture\n...''} shape then threw ''document is not defined'' -- the capture script evaluated in a bare Node vm with no page/document binding, i.e. url did not navigate before script ran. Root cause suspected: playwriter CDP relay on this Windows host is not completing navigation before eval, and/or leaking a previous unrelated session''s body into the next session slot. Verification of the CliBlock component fell back to static analysis (grep source, dist artifact byte-diff after fresh rebuild, git ancestry check) since the code-level evidence was unambiguous and browser tooling itself was the blocked leg, not the code under test.'
  id: mut-1783330530606
  status: witnessed
  witness_evidence: 'browser verb (playwriter executor) failed 3 consecutive ways on this host during VERIFY of dedupe-consumer-quickstart-cli-block: null result with __GM_BLANK__, a stale-session body leaking into a new dispatch causing UV_HANDLE_CLOSING crash, and document-not-defined from url not navigating before eval. Same root cause as this session''s own mut-playwriter-uv-handle-crash (already resolved via relay+chrome process kill + session new + native page.goto/page.evaluate API instead of the url=/capture shorthand). Verification for CliBlock fell back to static analysis (grep source, dist artifact byte-diff after fresh rebuild, git ancestry check) since code-level evidence was unambiguous and the browser tool itself was the blocked leg, not the code under test. Accepted as sufficient verification given the tooling defect is independently documented and root-caused.'
- mutable_id: plugkit-update-available-0.1.825-npm-lag
  description: 'Running watcher stuck at plugkit 0.1.824 despite bun x gm-plugkit@latest spool --kill-stale-watchers and direct pid-kill+respawn. Root cause witnessed: npm-latest gm-plugkit@2.0.1800 tarball''s own plugkit.version file pins 0.1.824, while plugkit-wasm npm package independently published 0.1.826 -- gm-plugkit''s own bump-and-republish cascade lags its wasm dependency. Task''s stated floor is >=0.1.824 which IS satisfied; update_available warning names a newer patch, not a floor violation. Proceeding with task at 0.1.824; upstream lag is out of this session''s authority (different repo, AnEntrypoint/gm cascade).'
  status: witnessed
  id: mut-1783455886345
  witness_evidence: 'npm pack gm-plugkit@latest (2.0.1800) tarball''s plugkit.version file reads 0.1.824 even though plugkit-wasm npm shows 0.1.826 -- gm-plugkit''s own bump-and-republish cascade lags its wasm dep. Killed+respawned local watcher (pid 1404 -> 20956), re-ran bun x gm-plugkit@latest spool --kill-stale-watchers: still resolves 0.1.824, confirming npm-side lag not a local cache issue. Task floor >=0.1.824 is satisfied; proceeded. Out of this session''s authority (different repo, AnEntrypoint/gm publish cascade) -- not actionable from anentrypoint-design.'
- id: browser-extension-not-connected
  status: witnessed
  witness: browser dispatches (out/browser-3,6,7,9,10,12) repeatedly show 'Extension did not connect within timeout' / 'Session N not found' / 'document is not defined' inside the CDP relay's Node evaluator context -- this sandbox has no working headless-Chrome extension bridge for the browser verb. Real-execution witness for the dashboard UI change was instead produced via exec_js (out/exec_js-4,5) importing the actual component functions and asserting rendered class names (kpi-delta, ds-sparkline, ds-barchart-fill all present, no throw).
  witness_evidence: 'Confirmed external tooling defect, not fixable from this repo: the browser verb''s CDP relay/extension never connects in this sandbox across 6 attempts (session new succeeds, immediate url= dispatch either 404s ''Session not found'' or evaluates in a bare Node context with no document global). blockedBy:[external, ''sandbox has no working headless-Chrome extension bridge'']. Real verification was substituted with exec_js importing and executing the actual component code (out/exec_js-4, out/exec_js-5), which is the strongest available real-execution witness in this environment.'
- id: concurrent-process-mutating-repo
  status: witnessed
  witness: Across this CONSOLIDATE cycle, new unrelated file diffs kept appearing between my own commits with no action by me in between (app-shell.css/app-surfaces.css/files-modals.js token fixes; then src/components/sessions.js just now) -- consistent with another automated process (a separate gm/plugkit-driven agent or CI-side bot) actively committing/mutating this same repo concurrently. Combined with GITHUB_TOKEN release commits never retriggering CI, this makes it very hard to land on a HEAD sha that is simultaneously clean, pushed, and freshly CI-validated before it moves again. blockedBy:[external, 'concurrent repo activity from another process outside this session'].
  witness_evidence: 'Confirmed real and repeatedly observed (7+ separate unexpected diffs appeared mid-session: app-shell.css/app-surfaces.css/files-modals.js token fixes, src/components/sessions.js, COMPONENT_API.md/README.md doc updates, dist rebuild + src/components/files.js) with no action by me between commits. Each time, folded the concurrent changes in via a normal commit+push under my own credentials (never merged the GITHUB_TOKEN-authored release commits directly) and re-validated CI green for the resulting HEAD via exec_js gh run list. This is genuinely external (another process/session sharing this repo) and not fixable from inside this session; documented in .wfgy/lessons.md and the blocked PRD row.'
