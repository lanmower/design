- id: refresh-visual-baseline-after-restyle
  witness_evidence: CI run 30505702231 (ci workflow) confirmed real, expected pixel diffs across every preview page (stamps, theme-ink, type-display, type-mono, type-prose, type-scale, wordmarks, etc.) -- all differences are the intended token restyle (old Acid Editorial baseline PNGs vs new webjsx-toolkit-look renders). Step is explicitly report-only/non-blocking (job still went green) but the baselines are now permanently stale until re-captured.
  subject: Run node scripts/visual-baseline.mjs update to re-capture visual regression baseline PNGs reflecting the new webjsx-toolkit-look tokens, then commit and push
  status: completed
  witness: 'Ran node scripts/visual-baseline.mjs update locally (fresh CDP session, port 50454) -- wrote 87 baseline PNGs, then ran check mode: OK, every page matches. git diff against visual-baselines/ shows ZERO byte changes versus the already-committed baselines. This confirms the CI run diffs were NOT a real content regression from the restyle (which was already captured in the committed baselines from this session own earlier local capture) but the documented cross-platform font-rasterization noise this gate is explicitly report-only for (matches prior recorded finding: Windows-captured baselines drift 0.7-2.5% on Ubuntu CI runners with no real layout change, pure font rasterization -- see rs-learn key mem-fb3c7ad25f0975c5). No baseline re-commit needed since local bytes already match committed bytes exactly.'
