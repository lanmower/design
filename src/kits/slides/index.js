// slides kit — presentation-deck web component (title/content/quote/
// comparison/receipt/end template pattern). Side-effect import registers the
// <deck-stage> custom element; DeckStage itself is also exported for direct
// use/typing by a consumer that wants the class reference.
export { DeckStage } from './deck-stage.js';
