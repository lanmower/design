
## Unreleased
- fix(shell): `AppShell` folding `topbar`+`crumb` into one `.app-chrome` band nested the whole self-wrapping `Topbar()` `<header class="app-topbar" role="banner">` inside the outer `<header class="app-chrome" role="banner">` — two stacked `<header>` landmarks ("two title bars"), visibly reproduced on 247420.xyz's homepage. `AppShell` (`src/components/shell/app-shell.js`) now detects when `topbar` is Topbar()'s own self-wrapped header and unwraps it (renders a bare `.app-topbar` div with its real children) before folding into `.app-chrome`, so exactly one `<header role="banner">` exists. Live-witnessed: rendered a real `AppShell({topbar, crumb, ...})` output via `applyDiff` in an actual browser DOM — `headerCount:1`, `bannerRoleCount:1`, `.app-chrome` children are `DIV.app-topbar`/`DIV.app-crumb` (were `HEADER.app-topbar`/`DIV.app-crumb` before the fix).
- chore(test): removed root `test.js` (440L) — mostly fixed unit-style assertions on component output shape (Pill/Pager/JsonViewer/Table), which is the test-suite-as-evidence pattern this project no longer permits; it was also unwired from CI (`ci.yml` never ran it), so nothing currently regresses from its removal. Real coverage gap left behind: the CSS-reference-vs-deploy-copy-list cross-check (`ui_kits/*/index.html` CSS links vs `gh-pages.yml`'s copy list) and the self-verifying type-staleness probe (mutates a signature, confirms `lint:component-types --check` actually fails, then reverts) were genuinely useful live checks, not suite assertions — worth re-authoring as standalone verify scripts (real execution, no fixed expected-output cases) in a follow-up pass rather than silently losing that coverage class.
- fix(os): restored `./kits/os/wm.css` (source file + package export), reverting part of the earlier "BREAKING: removed wm.css/launcher.css/validate.css" entry below. That entry's "theme.css already contains the same rules" claim does not hold for `wm.css`'s base geometry: every `.wm-win.wm-max`/`.wm-win.wm-min` rule in `theme.css` is explicitly an *override* on top of a base rule it assumes `wm.css` still provides (see its own "LOAD-BEARING !important: wm.css declares..." comments) — theme.css alone has no bare `.wm-win.wm-max{width:100%}` or `.wm-win.wm-min .wm-body{display:none}` outside its `@media print` block. Live-witnessed on `ui_kits/os/`: maximize only grew height (theme.css's partial override applied, wm.css's `width:100%!important` did not — window stayed 422px wide in a 1280px viewport) and minimize never hid the window body (`getComputedStyle(...).display` stayed `flex`), silently breaking both features since the March removal. Restored from the last pre-removal build (`dist/src/kits/os/wm.css`, tokens/zero-border-aesthetic already current) and linked before `theme.css` in `ui_kits/os/index.html` (load order matters: overrides must come after the base). `launcher.css`/`validate.css` stay removed — genuinely unreferenced, confirmed again.
- fix(gallery): the lightbox's own "close" button (`ui_kits/gallery/app.js`) called its `close` callback directly instead of routing through `Dialog`'s internal `close()` (`src/components/editor-primitives/modals.js`), which is what performs the opener-focus-restore (`queueMicrotask(() => opener.focus())`). Escape and backdrop-dismiss both restored focus to the originating `.ds-gallery-tile` correctly; the visible close button left focus on `<body>`. Fixed by dispatching a synthetic bubbling Escape `keydown` on the dialog root from the button's click handler, so all three dismiss paths now go through the same internal close.
- fix(settings): `loadDraft()` (`ui_kits/settings/app.js`) read the auto-saved `localStorage['settings-draft']` but was never called anywhere — a returning user with an unsaved draft from a prior session saw the original seed profile on reload with no indication a draft existed, silently orphaning the autosave forever. Added a mount-time check that surfaces a `RestoreDraftModal` (same `Modal` shell as the existing in-session `DiscardConfirmModal`, so it gets the same focus-trap/Escape/autofocus wiring) when a draft is found before first render.
- fix(signin): the post-submit success panel's title fell back to "welcome back" for both `signin` and `signup` modes — a newly created account was greeted as a returning user. Added a distinct "account created" title for `signup`; `magic`/`reset` were already distinct.
- feat(polish): full-fleet visual-quality pass across all 23 ui_kits, each independently graded against a premium composition bar (spacing rhythm, hierarchy, alignment, "designed vs assembled") then triaged and fixed where the finding survived live re-verification. Real fixes landed:
  - `blog`: `.app-main.narrow`'s wide no-sidebar column left `.lede`/`.ds-prose` edge-anchored inside their own reading-measure cap, stranding ~45% of the viewport as unbalanced dead space — centered them inside the wide column (`base.css`).
  - `homepage`: `.ds-hero + .panel` stacked its own top margin on top of the hero's already-96px trailing padding, landing the stat block ~138px from the hero instead of the ~42px every other panel transition uses — zeroed the panel's own margin so one side owns the gap (`hero-content.css`).
  - `community`: `.ds-community-msg-text` had no width cap, so short chat lines stretched across the full ~970px pane, reading as broken layout rather than intentional spaciousness — capped to 70ch (`kits-appended.css`).
  - `chat`: `.ds-chat-layout`'s hard `min-height: max(30vh, 420px)` floor could exceed the kit's available column height on a short viewport, so the header claimed "9 messages" while only 2 were visible and the internal scroll region never engaged — switched to `min-height: 0` so `flex:1` actually yields to the container and `.chat-thread`'s own `overflow-y:auto` is what scrolls (`kits-appended.css`).
  - `settings`: the profile panel mixes `DsField` (`.ds-field-wrap`) and a local `Field()` (`.ds-field-block`) with different vertical-rhythm rules, so name/email and handle/bio fields didn't share one spacing scale — unified both under the same margin rule (`kits-appended.css`).
  - `signin`: `.ds-auth-form` doubled its own responsive padding on top of the auth column's — removed the redundant padding (`kits-appended.css`).
  - `slide_deck`: rendered two "theme: auto" pills (`Topbar`'s default toggle plus its own `PageHeader` toggle) — opted out of `Topbar`'s via the existing `themeToggle: false` escape hatch (`ui_kits/slide_deck/app.js`).
  - `system_primer`: `.ds-prim-label`'s fixed 64px flex-basis (meant for short leading row labels) also constrained free-length trailing status text like "none pressed yet", wrapping it into a cramped 3-line stack — added `.ds-prim-label-status` for the trailing-text case (`primitives.css`/`kits-appended.css`/`ui_kits/system_primer/app.js`); the same kit's collab-overlay demo (`LiveCursorOverlay`/`RemoteSelectionRings`) had no positioned ancestor closer than `.panel`, so the `position:absolute; inset:0` overlays escaped their own demo row and covered the whole panel including its title — scoped via `.ds-prim-row-collab { position: relative }`; and `.ds-menubar`/`.ds-menubar-item`/`.ds-menubar-trigger` (real components, wired via `Menubar()`) had **no CSS at all**, falling back to stacked block-level UA-default buttons — added the missing rule set (`primitives.css`).
  - **Chat message component** (`src/components/chat/message.js`, shared by every kit that renders a `ChatMessage`): for incoming ("them") turns, `meta` (name/timestamp) rendered AFTER the bubble in DOM order while CSS displayed it above — so an incoming message's name line visually paired with the PREVIOUS bubble, and its own avatar looked orphaned beside its bubble with no name (`community-app`'s flagged "avatar alignment" finding). Reordered `meta` before the bubble for incoming turns only; outgoing ("you") turns are unaffected.
  - Confirmed false positives (investigated live, left unchanged): `dashboard`'s KPI trend-color semantics (the invert/danger-threshold logic already handles polarity correctly — a currently-green error-rate card is genuinely under its own danger threshold), `community-app`'s reported avatar-identity mismatch (each avatar correctly matches its own message per live `getBoundingClientRect` measurement), `settings`'s theme segmented-control "inconsistency" (the demo control and the global site theme badge are two intentionally separate, unrelated pieces of state), `signin`'s alignment claim (form card and heading share an identical left edge, mathematically confirmed).
  - Full re-verification after all fixes: lint 17/17, `lint:component-docs`/`lint:component-types` 0 drift, `lint:dead-controls` 0, a11y 23/23 kits clean, spot-checked 7 fixed kits live with no regressions.
- fix(topbar): the merged `.app-chrome` header (topbar + crumb sharing one sticky band) gave `.app-topbar` a hard `flex: 0 0 auto`, so any kit whose topbar content (nav links + a wide control like the search kit's `.ds-topbar-search`) exceeded the leftover width after the crumb wrapped the ENTIRE topbar onto a second line instead of letting its own children shrink — even though `.ds-topbar-search` already had a shrink-capable flex-basis, it was inert because the parent box refused to shrink first. Witnessed live on the `search` kit at a plain 1280px desktop viewport: crumb 564px + topbar 596px overflowed the 1184px content box, doubling the header's height and visually breaking the sticky band. Changed `.app-chrome > .app-topbar` to `flex: 1 1 auto; min-width: 0` (matching the crumb) plus `justify-content: flex-end` so its own children compress first; re-verified across 8 kits (search, dashboard, settings, docs, terminal, chat, community, system_primer) with 0 regressions, lint 17/17, a11y 23/23 kits clean.
- fix(states): `.loading::after` (button/field spinner) and `.skeleton` (loading placeholder) in `states-interactions.css` ran their `animation:` unconditionally with no `prefers-reduced-motion` guard — the one gap found in a full-codebase sweep (grepped every `animation:` site across all 9 CSS files that use one against their `prefers-reduced-motion` coverage; the other 7 were already fully guarded). Added the matching `@media (prefers-reduced-motion: reduce) { animation: none; }` block for both, following the codebase's existing pattern.
- fix(os): `.ds-247420 .wm-win` had no `position` at desktop widths — `position: absolute` was only set inside the two mobile/tablet `@media` bands, so on desktop the inline `left`/`top` bounds set by `wm.js` were silently ignored and every floating window rendered `position: static`, collapsing to the top of `.wm-root` with its titlebar clipped by the menubar. Live-witnessed via CDP (`getComputedStyle(...).position === 'static'`, rect `top: 8` vs the intended inline `style.top: 80px`) after the audit's own broken-URL false positive (below) was ruled out first.
- fix(a11y-audit): `scripts/a11y-audit.mjs` requested `/ui_kits/<kit>/index.html` directly; `http-server` 301s that to the extensionless directory path with no trailing slash (`/ui_kits/<kit>/index`), which then resolves the page's own relative `./app.js`/`<link>` one directory too high and serves a near-empty DOM. The audit was silently auditing broken pages every run — visible only as a suspiciously low per-kit rule/pass count (5 vs the real 15-30) — while still reporting a clean "0 blocking violations" against a real baseline of 0, a false-green CI gate. Switched to a trailing-slash directory URL; live re-run now shows every kit fully rendered (23/23, 0 violations, real pass counts) and CI's exact `http-server . -p 8899 -s` invocation was used to confirm the redirect (not just the local one-off flags used during triage).
- **BREAKING**: removed `./kits/os/wm.css`, `./kits/os/launcher.css`, and `./kits/os/validate.css` package exports (and their `src/kits/os/` files). All three were fully superseded by `theme.css` (which held a strict superset of their rules — 26 `.wm-win` selectors vs `wm.css`'s 11, 2 `.launcher-dock` selectors vs `launcher.css`'s 2) and unreferenced by any consumer; `validate.css` styled a `validator-app.js` component that no longer exists in this repo's source at all. If you imported any of these three paths directly, switch to `./kits/os/theme.css`, which already contains the same rules.
- fix(community): WCAG contrast fix for per-user hashed avatar initials — `.cm-user-avatar`/`.cm-member-avatar`/`.cm-voice-user-avatar`/`.cm-ch-voice-user-avatar` used one fixed `--fg-2` text color against an arbitrary per-user `--avatar-bg` hash, which measured as low as 3.4:1 against several hues (documented gap in a consumer's WCAG audit). Added `avatarContrastFg(color)` (`src/components/content/avatar.js`) computing black/white text via relative-luminance against the actual background, wired through `presence.js`/`navigation.js` as a `--avatar-fg` custom property alongside `--avatar-bg`; CSS now reads `color: var(--avatar-fg, var(--fg-2))`.
- feat(community): `PageView` now accepts `author`/`updatedAt` props and renders them in `.cm-page-head` as `.cm-page-meta` — a consumer previously had no way to surface page attribution even though the underlying data existed (documented gap in a consumer's audit).
- feat(overlay-primitives): `EmojiPicker` gained a real search `<input>` (`.ov-emoji-search`) wired to its existing (previously prop-only, never consumer-fed) keyword-filter — typing now filters the grid by emoji name directly inside the picker instead of requiring a consumer to route external text into the `query` prop.
- fix(lint): `src/kits/os/theme.css` had a raw `z-index: -1` on `.os-menu.open::before` (a below-content backdrop) that the widened `lint-zindex` gate now catches — repointed to the existing `--z-below` token (same value, named rung); allow-listed the file's intentional `--accent-primary: #247420` brand-lead literal (already documented as deliberate in the adjacent comment) so `lint-tokens` stops failing the build on a value that was never meant to be themed.
- chore(deps): update postcss 8.5.19 -> 8.5.25 -- lockfile had pinned an old patch within its own floating caret range.
- feat(freddie-chat): rewrite the `chat` page from SSE-over-POST to the freddie wire protocol over WebSocket (`/api/agent/stream?sessionId=`) — client-generated session ids so the socket subscribes before the first prompt, transcript rebuild from the server's wire-log replay on (re)connect (a refresh mid-turn loses nothing), interleaved live tool cards from real `tool.start`/`tool.end` event pairs (replacing the provisional tool_progress-only parts), mid-turn send = steer (injected at the agent's next step boundary), stop = `cancel` (INTERRUPT) instead of an HTTP abort, and no more 120s request ceiling on long agentic turns (WS turns run to `agent.turn_timeout_ms`, default 600s). The legacy POST `/api/chat` path remains only for the offline IndexedDB outbox flush.
- feat(components): add `ApprovalNode` (`chat-message-parts/agent-nodes.js`) + `approval` kind in `PART_RENDERERS` — renders the freddie wire protocol's `approval.request` as a tool-style card with args and approve/always/reject actions wired through `p.onResolve`, settling to approved/rejected state on `approval.resolved`. Browser-witnessed end-to-end in the freddie dashboard: card renders on a gated bash call, click resolves over WS, tool executes, card settles.
- feat(theme): restyle the whole token system to a webjsx-toolkit-derived shadcn-neutral look, replacing "Acid Editorial" — `colors_and_type.css` root palette rewritten to grayscale (`--paper`/`--ink`/`--acid` now neutral, not warm-paper/electric-lime), `--r-1/2/3` remapped to an 8px-based scale (from 10/14/22px), `--ease-drawer`/`--ease-collapse`/`--ease-out-strong` added (ported from webjsx-toolkit's `styles.css` `@theme inline` block), `--grain-opacity` zeroed (print texture confirmed unused by any live component, left defined for future opt-in). All semantic token NAMES and their `color-mix()`/`oklch()` derivation formulas are unchanged, so every consumer keeps working. Caught and fixed real regressions along the way, all WCAG-verified: `--accent-fg` was still the dark `--ink` tone while `--accent` itself had become dark-neutral (1.15:1 dark-on-dark across every `.btn-primary`/active-nav-item in every kit); `--ink-3`/`--paper-3-dark` tier-3 text tones sat just under 4.5:1 (darkened/lightened to clear 4.54:1/4.52:1); `.chat-empty-sub`'s `opacity:.7` blend fell to 3.72:1 (raised to `.82`); `.ds-cli-ok` used `--acid` directly on the always-dark `.cli` surface (repointed to a new fixed `--success-on-ink` token, following the existing `--mascot-on-ink` pattern). `npm run a11y`: 20/20 kits, 0 blocking violations.
- feat(theme): convert `.ds-hero` from an asymmetric two-column editorial grid to a centered flex stack, reversing the prior "no centered stack, ever" house rule as part of the webjsx-toolkit-look restyle — title/body/CTA row now center-aligned, the stat/badge aside card moved from a right-offset column to a full-width card below the body with its accent spine rotated top-edge. All child classes/markup unchanged; live-verified zero overflow and 0 new a11y violations.
- feat(fix): disable HTTP cache in `scripts/cdp.mjs`'s `withPage()` before navigation — a stylesheet served with a real `cache-control` header (any local http-server's default) was silently read from the Chrome profile's disk cache on a repeat navigation to the same URL after a source edit, so a live-verification script could read stale CSS even though `curl` and an in-page `fetch()` both saw the current bytes. Bit two separate verification passes this session before being root-caused and fixed.
- feat(components): backfill nine gaps found by a full webjsx-toolkit-vs-design component audit — Calendar/DatePicker/DateRangePicker (`src/components/calendar/`, controlled single+range date-grid, composes the existing Popover), Slider (`src/components/slider.js`, generalized from the existing `VadMeter` range-input styling), InputOTP (`src/components/content/otp-input.js`, real per-box inputs with auto-advance/backspace-retreat/paste-split), Carousel (`src/components/carousel.js`, native scroll-snap, no drag-library dependency), HoverCard (`src/components/overlay-primitives/hover-card.js`, composes Popover with delayed hover timers), Menubar (cross-menu hover-switch built directly on the shared roving-menu hook), Progress (extracted from `BarRow`'s fill approach, `src/components/data-density/progress.js`), ContextMeter/ContextTreemap/ContextXRayPanel (`context-pane.js` split into a barrel-over-submodules directory, treemap is a from-scratch squarified SVG layout), AspectRatio (native `aspect-ratio` CSS property, `.ds-aspect` utility). All follow house conventions (token-only CSS, `ds-*` classes, 200-line caps, JSDoc closed-enum prop types); `docs:components`/`types:components` regenerated, 0 drift.
- feat(components): add a real-time collaboration primitive group, `src/components/collab/` — `LiveCursorOverlay`, `RemoteSelectionRings`, `RecentEditHighlightFlash` (full-bleed pointer-events:none overlays), `AgentPresenceChip`/`PresenceBar` (reusing `community/presence.js`'s avatar-chip shape under a distinct `ds-collab-chip` prefix). New territory for this codebase — the only prior "presence" concept was Discord-style voice/member-list chips, not multiplayer co-editing UI.
- feat(system_primer): add a live "restyle 2026" showcase panel demonstrating the nine new primitives (Slider, Progress, InputOTP, HoverCard, DatePicker) with genuine wired state (not static screenshots) — matches the existing `PrimitivesPanel`'s interactive-demonstration standard.
- feat(system_primer): showcase the remaining twelve backfilled primitives in a second `BackfillPanel`, closing a real coverage gap — the entry above covered only 6 of the 18 backfilled symbols, and `DateRangePicker`, `Carousel`, `Menubar`, `AspectRatio`, the five `collab/` overlays and the three `context-pane` views had no live surface in any kit or preview page. Input-driven controls are wired to real state; the collab overlays and context views take representative fixture data, which is the honest specimen for a component whose real input is a remote peer's cursor or a token-accounting breakdown. Live-verified over CDP: the range picker opens a 42-cell calendar, the menubar opens a `role=menu` with four `role=menuitem`s, the treemap emits a real squarified SVG, and the selection ring measures exactly the geometry passed in. `system_primer` rises 26 → 30 axe passes, still 0 blocking.
- fix(collab): pass the nested `rect` shape the selection-ring and edit-flash overlays actually read — `LiveCursorOverlay` takes flat `x`/`y` but its two siblings in the same file read `rect.left`/`rect.top`, so the flat shape throws on a property of `undefined` during synchronous mount and blanked the *entire* kit (`#root` empty), not just the one specimen. The same wiring pass also caught the context views reading `value`, not `tokens`: a `tokens`-keyed fixture renders zero-width meter bars and an empty treemap with no error at all — the silent-wrongness shape this repo's own notes call its most-recurring defect.
- fix(preview): remove the animate.css CDN from 25 preview pages, style the `.row-code`/`.row-title`/`.row-meta` and `.chip.tone-dim` classes that had no rule anywhere, and fix the preview index — it used the FILL accent as link text (1.07:1 on paper, invisible in light theme) and had no `h1`.
- fix(preview,blog): stop the theme map scrolling sideways — its rows were a nowrap flex with two fixed `min-width` floors, so the unbounded consumers column pushed the page ~167px wide; and drop `.centered`, a no-op class duplicating what `.app-main.narrow` already does.
- fix(a11y): give the four kits that bypass `AppShell` a heading and a skip link — community, community-app, blog and docs inherited neither, so the community kits had no `main` landmark at all and a keyboard user tabbed through two full nav columns to reach content. Fixed in `CommunityShell`/`ChatHeader`/`community-app.js` so a fifth consumer cannot reintroduce it. Invisible to the CI a11y gate by design: it runs WCAG tags, and these are best-practice rules.
- fix(homepage): give the works and writing rows real destinations — no `works` entry carried `href`/`source`, so all 16 buttons in expanded works led nowhere, and all five writing links were literal `#`. The row this closed also claimed 20 of 20 controls were inert; re-probed correctly the figure is 0 of 22.
- fix(sdk): stop three components promising interactions they do not have — `Select` dropped the caller key on its bare-select path so two sibling label-less Selects collapsed into one; `Side()` defaulted `href` to `#`, making the dead affordance the DEFAULT for any item missing a destination; `.cm-server-header` carried `cursor:pointer` and a hover state on a static div. Also stops serving `ui_kits/_template/index.html`, which answered 200 while full of raw `{{TITLE}}` placeholders.
- fix(theme-toggle): make the control show the theme it just set — clicking "light" flipped `<html data-theme>` but left `is-on` on whichever mode was selected at mount, because nothing re-renders this component on a theme change and the `refresh` Set meant to cover it was never populated. The compact variant additionally computed its next theme from a stale captured value, so its cycle stuck after one click.
- fix(blog,docs): replace 24 placeholder links with real ones or none — 16 of 20 controls on docs and 6 of 9 on blog did nothing. Eight had real destinations (four in-page anchors, plus repo links); the rest name sections these single-page kits do not contain and now keep their row but lose their href rather than faking a destination.
- feat(lint): gate controls that render but cannot act — `lint-dead-controls` fails the build on an empty handler body or a bare `href="#"`, the two shapes that let 30 dead controls ship. Proven to FAIL before being trusted, and it immediately caught six placeholder hrefs missed by hand.
- feat(preview): render real components instead of imitating their markup — only 1 of 29 preview pages actually imported a component, so "coverage" measured hand-copied HTML that drifts silently. Two new specimen pages take real rendered coverage from 11 of 219 exports to 63. Writing them against the real components caught four wrong prop names and a real SDK bug: `PageHeader`/`Panel`/`Section` rendered `id="null"`, because webjsx stringifies a null prop rather than omitting the attribute.
- docs: regenerate `component-props.md` after the module splits — the generator resolves each symbol by matching the first `const <name> =` in the barrel-target file, and locals named `sessions`/`skills`/`tools` inside the old monolithic `freddie.js` home render body shadowed the real page definitions; splitting removed the ambiguity. Still 247 symbols, 0 drift.
- refactor(freddie): finish the pages-config split, so the last freddie page module lands under the documented 200-line cap.
- refactor(components): split the last oversized modules and regenerate docs — `chat-message-parts`, `voice`, `chat-minimap`, `page-html` and the freddie chat page become thin barrels over single-responsibility modules, taking files over the 200-line cap from 18 to 7 with no public export moving. Every barrel uses import-then-bare-export because a pure `export … from` makes the doc generator rewrite with drift warnings.
- fix(css): apply the scrollbar treatment once at the root, not per component — only `chat.css` styled its own scrollers, so 4 of 4 containers on dashboard and 7 of 7 on gm_inspector fell back to a UA default that reads as a bright seam on a near-black panel.
- fix(a11y): floor the base `.input` and `.skip-link` to 44px touch targets — the primitive every form actually uses rendered 34px and the skip link 39px, measured under real coarse-pointer emulation; the skip link is floored unconditionally because switch and head-pointer users are not reported as coarse. `lint-fontsize` gains a token-anchored `max()`/`min()`/`clamp()` exemption so the iOS-auto-zoom `max(16px, var(--fs-body))` floor is not pushed toward a strictly worse bare `16px`.
- feat(lint): add `zindex`, `transition-all`, `fontsize` and `important` gates and migrate 47 font-sizes — takes the lint surface from 8 gates over 9 sheets to 13 over 30; the two hard gates are zero-tolerance because their corpus is genuinely clean, the two ratchets are debt to drive down and never a budget to raise. Also plugs a real hole: `lintSpacingOrThrow` ran outside the `CHECKS` loop under a stale report-only comment, so a genuine spacing regression escaped as an unhandled exception instead of a reported failure.
- docs(css): record why the changelog-row `!important` must stay — tested rather than assumed; removing it collapses the version column to 0px at tablet width, because a changelog row carries three tracks where a plain `.row` carries two and the responsive blocks legitimately rewrite `.row` later in the cascade.
- fix(a11y): define `.sr-only` in `app-shell.css` and add the missing `.ds-prose-endnote` rule — both were emitted by real components but defined only in sheets most kits never link, so any consumer using them rendered screen-reader-only text visibly. Verified against the real accessibility tree, not computed style.
- fix(a11y): use `--accent-ink` for accent text — `.ds-ep-tab.active` and `.ds-ep-tree-item.selected` used the fill tone as text, measuring 1.07:1 on paper against a 4.5:1 floor; the bug survives review because both tones resolve identically in dark theme.
- test: invert the `ds-panel-trio` guard now that the class has a real rule — the assertion had inverted and was failing on correct code; it now requires three real grid tracks and fails if the percentage-span form (which cannot express an equal 3-up with gaps) returns.
- refactor(css): migrate 139 more spacing literals and fix app typescale reach — 366 to 227, proved by resolving every token back to px and diffing the multiset of all 413 declarations, so no computed value moved. `.ds-app-surface` set the app heading scale with a direct-child selector, so a kit wrapping its title in a head row opted in but never got the rule.
- fix(kits): link the stylesheets each kit actually needs — `ui_kits/community` never linked `community.css`, so its `.cm-server-rail` computed to 1425px instead of 72px and the entire shell chrome rendered unstyled. Changes route through `kits.config.mjs` and the scaffold generator, never hand-edited generated files.
- fix(a11y): guard the last unguarded animation behind `prefers-reduced-motion` — found by parsing brace depth rather than counting guards per file; all 62 animation declarations now respect the preference.
- fix(lint): compare token values whitespace-insensitively, unblocking CI — the multi-line `--shadow-*` tokens reported permanent drift because the line ending was part of what was compared, so the gate passed on a CRLF checkout and broke both the ci and npm-publish workflows on LF.
- feat(css): add sub-12px type tiers, unify the `.t-label` voice, style 13 dead classes — `--fs-pico` (10px) and `--fs-nano` (11px) close the gap below the scale floor that was blocking the last raw font-sizes; nine selectors had hand-reimplemented the `.t-label` recipe and drifted into five letter-spacing values and two sizes.
- refactor(os): fix the specificity war in `theme.css`, 128 `!important` to 60 — root cause is that `src/kits/os/theme.css` ships unscoped and competed with `wm.css` at equal specificity in undefined load order, so `!important` was the only thing making the outcome deterministic; scoping to `.ds-247420` wins order-independently. The 60 that remain each carry a comment naming what they override.
- refactor(css): migrate 280 raw spacing literals onto the `--space-*` scale — 646 to 366, and corrects six `var(--space-N, <fallback>)` declarations whose fallbacks did not match the token, latent bugs invisible to the gates that would shift layout for any consumer loading a component sheet without `colors_and_type.css`.
- fix(gui): close the lint blind spot, restore barrel completeness, introduce the `--z-*` scale, cut eyebrow grammar — the token lints scanned `app-shell.css`, which is only a 24-line `@import` barrel, so ~5,300 lines under `src/css/app-shell/` were never linted; the gate now resolves imports transitively and hard-fails on any unreachable sheet in that directory. That guard immediately exposed four sheets bundled into `dist` but never imported by the barrel, so `<link>` consumers rendered those components unstyled. Two contradictory z-index scales are replaced by one 13-rung `--z-*` ladder with 70 raw literals migrated to zero, and 34 rendered eyebrows drop to 4 because a category label with a constant value across every sibling orients nobody.
- fix(layout): correct inverted panel rhythm, wire density end-to-end, restore the SessionRow layout floor — outer panel separation was 8px against a 16px inner sibling gap, so unrelated panels read as more tightly grouped than related content; `--density` scaled only `--pad-x` and `.row` padding, so panels, fields and bars held still; and SessionRow's base layout lived only in `chat.css`, which one kit links, so in gm_inspector all row children collided at 0px.
- chore: ignore `.gm/browser-chrome-profile*` runtime dirs, which the browser verb's newer naming variant left as untracked residue on every status check.
- feat(sdk): **per-component size API + density that reaches control internals** — the second, deeper half of the webgeist space-optimization port. The prior pass added new *components*; this pass fixes the front-door components (`Btn`/`TextField`/`Select`/`Chip`/`Badge`) that still defaulted to marketing scale with no size lever, the exact gap that made the system look under-refined next to the local components. New `--ctl-sm`/`--ctl-md`/`--ctl-lg` control-height ladder (28/34/42px, density-scaled) + `--field-height`/`--row-height` primitives in `colors_and_type.css` — the missing root that let control heights drift across 20/24/26/28/32/40/44px. Added the missing `--space-2-75` (12px) and `--space-3-5` (20px) rungs that the 10→16→24 holes were forcing components to hardcode. `Btn` gains `size: 'sm'|'md'|'lg'` (`.btn-sm`/`.btn-lg`, fixed height off the ladder + horizontal-only padding, replacing the preview's faked `style="padding:5px 10px"` sizes); `TextField`/`Select` gain `size` (`.ds-field--sm`/`--lg`); `Chip` gains `size` + a rectangular sentence-case `tag` variant for dense data (the all-caps pill was shouty in table cells); `Badge` gains `size`. Unified the three divergent input idioms — the boxed `.ds-field` control now snaps to `--field-height` with a tighter `--r-1` (10px) corner (was `--r-2`/14px), and bare `<input>/<select>/<textarea>` finally get the editorial tonal-fill baseline (were browser-default white boxes; the `:not()` guards are wrapped in `:where()` so they keep zero specificity and don't out-rank the `.ds-field` size rules). `[data-density]` now actually moves front-door internals: buttons/rows/table-cells scale via the `--ctl-*` ladder + `calc(token * var(--density))`, so `compact` drops a button 34→26px and a table cell 8→6px (was a near-no-op that only touched `--pad-*`). Tightened marketing-scale defaults to app density: `.row` defaults to 12/16px + `fs-sm`/500 title (18px/600 moved to opt-in `.row--lede`); generic `table` cells 12/14→8px + radius `--r-3`(22px)→`--r-0`; `.kpi-card` 32→16px pad, number 48→32px; `.empty` 96→48px; sidebar nav items 44px pill → 34px `--r-1` rect (44px restored under `pointer:coarse`); check/radio/toggle rows 44→30px desktop (44 under `pointer:coarse`). Added a `Tabs` animated sliding underline indicator (a real child element sized by left+right insets — an abspos element's inline `width` collapses to 0 in some flex-sibling layouts — replacing the static per-tab border toggle). Added a `.panel--dense` variant and a shared `.ds-catalog`/`.ds-demo` preview shell (hairline flex-wrap gallery cell + 240px sticky sidebar) so previews stop hand-rolling layout `<style>`. Consolidated 14 raw `outline:2px solid var(--accent-ink)` focus declarations (11 in `community.css`, 3 in `colors_and_type.css`) onto the `--focus-*` tokens; snapped `.ds-dropdown-trigger`/`.ds-ep-actionbar button`/`.ds-ep-dialog-btn` to the `--ctl-sm` rung and tokenized dialog/toast padding. Every change rides the existing acid/ink/paper palette and `--space-*`/`--r-*`/`--ctl-*` tokens (all 6 linters green, spacing-literal count down 624→612); verified live via `browser` witnesses (button/field size ladders render 28/34/42px, density shrinks internals, tabs indicator slides between tabs, all controls recolor under `data-theme=ink` + `data-accent=purple`).
- fix(ci): `.github/workflows/gh-pages.yml`'s static-CSS copy list was badly incomplete — only `app-shell.css`/`colors_and_type.css` ever shipped to the deployed site, silently 404ing 5 other real, actively-referenced stylesheets: `editor-primitives.css` (just re-linked in `dashboard`/`signin`, so that fix was live-broken until this commit), plus 4 pre-existing bugs unrelated to this session's own edits — `app-surfaces.css`/`chat.css` (referenced by `ui_kits/workspace`) and `community.css`/`community-app.css` (referenced by `ui_kits/community-app`). Found by fetching the deployed CSS URLs directly and getting 404s despite the source-level HTML link being correct — a class of bug no source-only check can catch, since the workflow's copy list lives outside any file a component-level lint would touch. Fixed the copy list comprehensively (all 9 root CSS files, not just the one bug in front of me). New `test.js` guard cross-references every `ui_kit/index.html`'s referenced CSS filenames against the workflow's copy list, so a future new stylesheet reference can't silently repeat this.
- fix(kits): `dashboard`/`signin` `index.html` never linked `editor-primitives.css` despite their `app.js` using `Grid`/`GridItem`/`Divider` (added in the prior commit) — the components rendered real DOM structure but with zero CSS, invisible to anyone actually loading the page. Found by fetching the live deployed site and checking rendered output rather than trusting the source-level fix alone. Added both missing `<link>` tags (matching the correct pattern already present in `gm_inspector/index.html`) and a systemic `test.js` regression guard scanning every `ui_kit` for `editor-primitives.js` component usage against its stylesheet links — word-boundary-anchored after an initial version false-positived on `FileGrid(` substring-matching a bare `Grid(` pattern.
- fix(kits): real adoption pass for the new space-optimization primitives (`Grid`/`GridItem`/`Divider`), not just demo-panel existence — audited every `ui_kit` + `site/theme.mjs` for a genuine fit. Fixed two real bugs found in the process: `ui_kits/dashboard/app.js`'s `environment`/`recent events`/`changelog` row used a `ds-panel-trio` class with **zero matching CSS anywhere in the codebase** (confirmed via full-repo grep), so it was rendering completely unstyled — replaced with real `Grid`/`GridItem` (`xs:true md:4`, 3-up responsive). `ui_kits/signin/app.js` hand-rolled a labeled OR divider (`ds-auth-divider` + two `ds-auth-divider-line` + a span) between the password form and GitHub/Google/SSO buttons — replaced with `Divider({label:'or'})`, dead CSS removed. Investigated and correctly declined several superficially-similar candidates rather than forcing a bad fit: the homepage `Kits`/`Decks`/`FileBrowser`/`Docs` sections render full-width `RowLink` rows whose internal `grid-template-columns: minmax(80px,12ch) minmax(0,1fr) auto` layout would cramp inside a narrow `Grid` cell; `settings/app.js` turned out to be a `Side`-nav single-section view (not stacked panels) where a `CollapseGroup` would regress working navigation into an accordion; `ui_kits/docs`'s sidebar is a static buildless HTML template with no JS component usage; `gm_inspector`'s own layout already uses the purpose-built `StatsGrid`/`ds-panel-duo` appropriately; `FileGrid` is a genuinely distinct file-browser component, not a `Grid` duplicate; chat.js's tool-call/thinking collapse widgets are native `<details>`/`<summary>` with chat-specific styling, already the right primitive. 3 new regression-guard checks added to `test.js` (35/35 passing) preventing the dead class and hand-rolled markup from silently regressing back in.
- feat(sdk): add `Grid`/`GridItem` (24-column responsive layout), `Collapse`/`CollapseGroup` (progressive-disclosure accordion), `Divider` (plain/labeled/vertical rule), extend `Pager` with an opt-in `numbered` mode (ellipsis-compressed page-button row, `siblingCount` prop), and extend `Table` with opt-in `striped`/`compact` density props — closes the space-optimization gap set found porting the `webgeist` project's (a from-scratch geist-ui port onto webjsx custom elements) dense-UI component patterns into this SDK. `Grid`/`GridItem` mirror the existing `BP_SM`/`BP_MD`/`BP_LG`/`BP_XL` breakpoint tiers via CSS custom properties + media queries, no new JS `matchMedia` infra. `Collapse`/`CollapseGroup` follow the existing `Drawer`/`Dialog` controlled-component convention (`expanded`/`onToggle`, no internal state). `Pager`'s default prev/next contract and `Table`'s default contract are both byte-compatible; `numbered`/`striped`/`compact` are additive opt-ins. All new CSS routes through the existing `--space-*`/`--r-*` token scale, zero new raw-literal lint violations. Demonstrated live in `ui_kits/gm_inspector`'s new "screen-real-estate primitives" panel.
- style(sdk): removed the AI-generated-landing-page tells from the homepage hero — the `.ds-hero::before` radial accent-ink glow wash and the `.ds-hero-aside::after` halftone dot-grid overlay (plus the now-orphaned `.ds-halftone` utility and `--halftone-size` token) are gone; the hero aside card is now a flat panel with just its lime spine, closer to Vercel's restrained, texture-free surface language than the print-gimmick look. Replaced the homepage `C.Marquee` copy ("always open" / "24 7 420" / "the creative department" / "shipping in public") — generic AI-slogan filler — with plain facts about the system itself ("open source" / "design tokens" / "buildless components" / "zero raw color literals"). `.ds-grain` (the subtle fractal-noise texture, opacity 0.05) is untouched — it reads as print grain, not a dot-pattern cliché.
- style(sdk): extend the Vercel-grade elevation language past `.btn`/`.panel` to the rest of the interactive surface — `.ds-panel-trio`/`.ds-panel-grid` card children get a restrained `--ease-spring` hover-lift (`translateY(-2px)` + `--shadow-2` + `--rule-strong` border), interactive `.row`s (`a.row`/`button.row`/`.row[role=button]`) gain a hairline `--shadow-1` hover (static non-interactive rows unaffected), and the homepage `.tabs[role="tablist"]` links gain the same spring lift. Added a single restrained `.ds-hero::before` radial depth wash (`color-mix(in oklab, var(--accent-ink) 10%, transparent)`, z-index -1, under the existing grain/halftone texture) for Vercel-style ambient depth without breaking the flat Acid Editorial print aesthetic — token-only, no new raw literals. Confirmed the site's focus-ring (`colors_and_type.css` `--focus-color`/`--focus-w`/`--focus-offset`, global `:focus-visible` rule) and display-type negative tracking (`--tr-tight`/`--tr-tighter`) were already Vercel-grade from prior sessions, so left untouched.
- fix(kits): full ui_kits audit sweep (all 14 kits) — fixed `Topbar` stringifying a controlled `search` VElement prop into literal `[object Object]` text; `ui_kits/docs` missing the shared off-canvas sidebar markup (`.app-side-shell`/toggle/scrim), so mobile nav rendered on top of main content; duplicated "signin · signin" breadcrumb; unstyled native checkbox clashing with the lime accent (`accent-color` now token-driven); clipped mobile nav labels on `project_page` (added trailing mask-fade so it reads as scrollable, not broken); filename/action-icon collision on `file_browser` at mobile width.
- fix(preview): full preview-page audit sweep (all 24 pages) plus `slides/` — several preview pages referenced CSS classes with zero rules anywhere in the stylesheet, silently falling back to browser defaults: `.dateline`/`.spread`, `.input`, `hr.rule`/`.rule-double`/`.rule-dotted`, `.stamp` + color variants + `.btn-stamp`, `.prose`. Added the missing rules. Also fixed stale content (spacing.html step-count mismatch, type-display/type-mono captions still naming pre-system-font webfonts, a wrong `--mascot` hex swatch, a `--link` token label that doesn't exist).
- fix(site): homepage polish sweep on anentrypoint.github.io/design — retinted sidebar count badges off a competing pink/magenta onto neutral+active-lime tokens (root cause was `.app-side .count` hardcoding `var(--mascot)`, not the per-row YAML `color:` fields, which were dead code and removed from `home.yaml`); fixed `.ds-hero` CTA row being bottom-aligned mid-copy instead of grouped under it; styled the previously-unstyled homepage `.tabs[role=tablist]` (was rendering as glued-together text); fixed the breadcrumb overlapping the sidebar-toggle button at tablet width; and wired `hero.badges` (defined in `home.yaml`, never rendered) into `C.Hero`/`HeroFromPageData` as a `.ds-hero-aside` lime-spined halftone card, giving the asymmetric Hero's right column real visual weight instead of dead space.
- style(sdk): `.btn`/`.btn-primary`/`.btn-ghost`/`.panel` in `app-shell.css` gain Vercel-inspired crisp elevation — hairline `--rule` borders, restrained `--ease-spring` hover-lift with `--shadow-1`/`--shadow-2`, softened active-press — refining the existing Acid Editorial button/panel chrome with token-only additions (no new literals). Also fixes a `fs_write` verb defect discovered mid-session: its content-payload key is `content`, not `body` (`body` silently wrote 0 bytes).
- feat(sdk): `Kpi` gains an optional third tuple element `{delta, tone: 'up'|'down', spark}` per item — a trend delta pill (arrow + text, `--success`/`--danger` toned) and/or an inline `Sparkline` line, both additive so every existing 2-tuple `Kpi({items:[[n,label]]})` call site is untouched. New `Sparkline({values, width, height, tone})` (pure inline SVG, `stroke: currentColor`, token-toned) and `BarChart({items:[{label,value,display}]})` (horizontal token-only progress rows, fill width driven by a `--bar-pct` custom-property write, never a raw inline `width:`) round out a small dashboard-chart vocabulary inspired by the finbro-dashboard reference. `ui_kits/dashboard/app.js` now shows delta+sparkline KPI cards and a `BarChart` traffic-by-channel panel instead of plain stacked numbers.
- feat(sdk): `JsonViewer` gains opt-in `mode: 'highlight' | 'tree'` and `copyable` props — highlight tokenizes JSON into `ds-ep-json-k/s/n/b/z` theme-token-colored spans via a linear no-regex scanner (non-JSON strings fall back to plain text, never falsely tokenized); tree renders nested objects/arrays as native collapsible `<details>` nodes with child-count tags (open above `treeDepth`, default 2); copyable adds a clipboard button with transient copied/failed feedback. Default `'plain'` mode is byte-compatible with the previous contract (raw-string `children[0]`, string input verbatim) so no existing consumer changes; backing CSS added to `editor-primitives.css` on the token+fallback idiom. Driven by gmsniff's JSON-sugar pass, which consumes the new modes across its GUI panels.
- feat(sdk): add `Pill` (shell.js), `Pager`, `JsonViewer`, `ToolbarRow`, `PropertyGridRow`, `InlineEditableField` (editor-primitives.js) — closes the gap set found while auditing gmsniff's GUI, which already consumes this SDK's AppShell/data-density/editor-primitives directly but had to hand-roll a `gui-extra.css` (table/pager/JSON-viewer/pill/toolbar/inline-edit chrome) for capabilities the SDK didn't yet ship. `Pill` wires the previously-orphaned `.ds-pill` CSS rule (scaffolded in an earlier session, never given a JS factory) to a real component, and `data-density.js`'s `TreeNode`/`DevRow` now call it instead of duplicating the inline markup. `ToolbarRow` is the flat variadic-row shape gmsniff's own panels.js comment says the existing `Toolbar` (leading/center/trailing 3-slot) doesn't cover.
- feat(tokens): extend `--space-*` scale with micro-tiers below `--space-1` (`--space-hair` 2px, `--space-half` 3px, `--space-1-5` 5px, `--space-1-75` 6px, `--space-2-5` 10px), mirroring the `--r-hair`/`--r-0` radius-scale precedent, so dense editor/inspector chrome has a real token target instead of none. All CSS added this session for the new components migrated onto the extended scale; `lint-tokens.mjs` spacing report holds at the pre-session baseline (641) despite the new rules. Full-codebase migration of the ~641 pre-existing raw-spacing sites across all 9 component sheets is explicitly out of this session's scope (tracked as a dedicated follow-up, same shape as the earlier radius migration but ~8x the site count).
- chore(test): add root `test.js` — mock-free real-services witness (real `scripts/build.mjs`/lint child-process runs + real ESM import/invocation of the new components), satisfying the project's standing single-test-file convention which this repo had not yet adopted for itself.
- docs: `COMPONENT_API.md` entries for `Pill`, `ToolbarRow`, `PropertyGridRow`, `InlineEditableField`, `Pager`, `JsonViewer`.

## 0.0.246 (unreleased)
- fix(a11y): dead sidebar hash-anchors on the SDK's own landing page — `site/theme.mjs`/`site/content/pages/home.yaml` sidebar bins (pinned/shipped/drafts) and tabs pointed at `#kits`/`#decks`/`#previews`/`#docs`/`#all` with no matching `id=` anywhere in the rendered output; added `id` to the corresponding `C.Panel` calls and an `id="all"` wrapper on the main content div.
- fix(page-html): wire section/heading ids so hash-anchor CTAs resolve — `renderPageHtml`'s shared `sectionNode()` never passed `sec.id` into `C.Section`, and neither the server `renderMarkdown` nor its client-side `__md` duplicate emitted `id=` on generated headings, so any consumer's `href="#some-heading"` CTA (freddie included) was structurally dead. Root cause was in the shared SDK renderer, not any one consumer's content.
- feat(sdk): add `HeroFromPageData`, `PanelFromItems`, `CliBlock` shared component factories plus `escapeHtml`/`escapeJson` exports (new `src/html-escape.js`) — consolidates hero-rendering, items-to-RowLink-panel, and quickstart-cli-block logic that four sibling consumer `theme.mjs` files (247420, zellous, wireweave, thebird) had each independently hand-rolled. Additive exports only this release; consumer repos migrate in a follow-up pass once published.
- fix(export): `ThemeToggle` was missing from the SDK root barrel (`src/index.js`) despite being a real component — consumers importing it from the package root got `undefined`.
- fix(lint): two hardcoded `'Inter'` font-family literals in `slides/index.html` bypassing the `--ff-body` token, fixed to reference the token.

## 0.0.243 (unreleased)
- feat(design): widen page stage width, extend radius token scale, add wide-viewport panel density utilities — decouples `.app-stage`'s max-width from Hero's `--measure-wide` (940px) via a new `--stage-wide` token (`min(96vw, 1440px)`), reclaiming real estate on every flatspace-consumer page without disturbing Hero's editorial measure or prose lede width; adds `--r-hair`/`--r-0` micro radius tokens and migrates all 76 raw `border-radius` literals onto the `--r-*` scale, then promotes the radius lint from report-only to an unconditional build gate; adds `.ds-panel-trio`/`.ds-panel-grid` auto-fit wide-viewport density utilities.
- fix(fonts): remove all custom web fonts, system-font stack only — `--ff-display`/`--ff-narrow`/`--ff-body`/`--ff-mono` now resolve purely to `system-ui`/`ui-monospace` fallback chains (no Bricolage Grotesque/Hanken Grotesk/JetBrains Mono `@import` or `@font-face`), removing the Google Fonts network dependency entirely; also fixed 3 downstream files with hardcoded custom-font literals bypassing the token (`site/theme.mjs` FOUC fallback, `src/kits/os/theme.css` freddie-chat rules, `src/kits/os/freddie-dashboard.css` mono fallbacks).
- feat(router): add `src/router.js` (`Router`/`createRouter`) — hash-based SPA routing over `applyDiff`, ported from the 247420.xyz consumer's hand-rolled copy (the only generic GUI-mechanism code found there; everything else in that repo's `lib/` is site-specific content/domain logic and stays put). Hardened over the original: an unregistered route renders an optional `fallback` component instead of throwing, the SDK-load wait is bounded instead of an infinite `requestAnimationFrame` retry, and router state is exposed via `debug.js`'s `register()` (`window.__debug.router`) instead of a raw `Object.defineProperty`. Fixes the bug class behind "a link's hash changes but the page doesn't" — an unmatched hash used to silently default to a guessed route.

## 0.0.239 (unreleased)
- fix(fonts): replace AI-tell fonts, maximize screen real estate on all devices — Space Grotesk and Inter were flagged by 2026 design discourse as AI-page-builder tell fonts (Space Grotesk paired with Instrument Serif, Inter/system-ui as the default AI-slop body face), so `--ff-display`/`--ff-body` swap to Bricolage Grotesque/Hanken Grotesk; a parallel screen-real-estate sweep fixed a hard 1400px ultrawide cap on `.app`, a hard 920px cap on the file_browser page stage, a dead mobile-menu button in community-app, small fixed window-spawn sizing in the os-kit window manager, and a letterboxed slide-deck canvas, while confirming every other ui_kit was already fluid.

## 0.0.227
- Jank sweep across `src/kits/os/` (theme.css, wm.js, launcher.css/js) and `src/kits/os/freddie/`: removed a dead unused `renderChatMessages(container, messages)` export in `src/kits/os/freddie/helpers.js` (superseded by the `<ds-chat>` custom element path; a same-named but distinct function in `src/components/freddie/helpers.js` is still live and untouched). Pruned a stale historical comment in `theme.css` referencing an already-removed legacy `.wm-snap-preview`/`.wm-switcher-row` block. Fixed an inconsistent CSS custom-property fallback on `.launcher-btn:active` (now matches the sibling `.active` rule's `var(--panel-select, var(--os-accent-soft))` fallback instead of failing silently when the token is unset).
- Accessibility: window control buttons (minimize/maximize/close) in `wm.js` and dock buttons (new-instance/select-instance/close-instance) in `launcher.js` now carry `aria-label` alongside their existing `title`, so screen readers get real labels instead of bare glyphs (×, −, +, x).

## 0.0.115
- theme.css: Space Grotesk + JetBrains Mono defaults (replaces Nunito/Archivo Black) so consumers no longer carry a local font override.
- theme.css: translucent menubar/taskbar (color-mix + backdrop-filter blur(10px)).
- theme.css: `[data-theme="ink|paper"]`, `[data-density="compact|comfortable|spacious"]`, `[data-accent="green|purple|mascot"]` selectors so consumers can theme via root attributes (intent ported from anentrypoint-update colors_and_type.css + system.css).
- freddie-dashboard.css: chatlog backgrounds use design tokens (`--panel-2`/`--panel-3`/`--panel-accent`/`--warn`) instead of `rgba(...)` literals; `font-family: var(--ff-mono, ...)`.

## [0.0.99]
- Split `src/desktop/freddie-dashboard.js` (was 655L with a 561L `createFreddieDashboard` and an inline `PAGES` map of 15+ route handlers) into a slim shell (~100L) plus four page modules under `src/desktop/freddie/`: `routes.js`, `helpers.js`, `pages-core.js` (projects/home/sessions/agents/logs), `pages-chat.js`, `pages-tools.js` (analytics/models/cron/skills/config/env/tools/batch/gateway), `pages-os.js` (instances/windows/x/fs). Each module is ≤ 200L per project convention. The 64-symbol-surface inner closure is replaced with a single `ctx = { instance, osSurfaces, root, state, rerender, host }` threaded into each page factory. Public export `createFreddieDashboard({ instance, bootHost, osSurfaces })` unchanged.
- Strip inline `style="..."` strings from freddie-dashboard.js per AGENTS.md inline-styles ban. The chat composer's gridded fields, the chatlog message rendering (text + tool-call disclosure), the env-chip wrap, and the provider-chip wrap all move to `.fd-*` classes in `src/desktop/freddie-dashboard.css`. Chat composer field rhythm now uses `var(--tool-gutter)` from the v0.0.97 rhythm refactor.
- Consolidate `Row` + `RowLink` in `src/components/content.js` into a single parameterized `Row` primitive that accepts `href`, `kind`, `cols`, `leading`, `trailing`, `target` in addition to the existing `code/title/sub/meta/active/onClick`. `RowLink` is retained as a thin backwards-compat wrapper calling `Row({ kind: 'link' })` so external consumers (e.g. ui_kits/error_404, src/components/freddie/pages-config) keep working unchanged.
- Document deferred items 3 + 4 from the prior session's audit as **false positives** after on-site inspection. `src/components/freddie/pages-chains.js:32` is `fetchJson('/api/acptoapi/chains/' + encodeURIComponent(name), { method: 'DELETE' })` — a URL fetch (not SQL), with the path parameter URL-encoded. `src/app.js:85` is `trail: ['247420', 'gm']` — a UI breadcrumb literal, not a secret. No code change.

## [0.0.101]
- Long-tail prose chrome strip: Manifesto in `ui_kits/homepage/app.js` no longer wraps in a `.panel` — now renders as bare `.ds-prose .ds-manifesto` per the v0.0.97 prose rhythm contract. Section padding moves from inline-style to `.ds-manifesto-section` class.
- Works expanded-body in `ui_kits/homepage/app.js` migrates from a 5-property inline-style div to existing `.work-detail` + `.ds-prose .ds-work-body` + `.ds-work-actions` classes (already defined in app-shell.css for the project_page kit). Action-row gap now inherits `var(--tool-gutter)` via `.ds-work-actions`.
- `ui_kits/blog/index.html` post body now carries `.ds-prose`; final endnote paragraph swaps inline-style for new `.ds-prose-endnote` utility class in app-shell.css.
- `ui_kits/docs/index.html` main prose wrapped in `.ds-prose` (split across the note panel so the prose surface stays contiguous within each editorial block). Note-panel body inline-style replaced with `.ds-pattern-notes` class; max-width inline-style replaced with new `.panel.panel-inline-wide` class. Crumb `margin-left:auto` inline replaced with `.crumb-right` class.
- Skipped: `ui_kits/{dashboard,error_404,gallery,project_page,settings,signin}/app.js` retain ad-hoc inline styles. These are showcase-only kits outside the SDK render path; their gap and inline-style strip belongs in a dedicated kit-rewrite pass with visual diff witness — applying surgical class edits without a browser-witness in this scope risked silent layout drift. Tracked for a follow-up.


- Prose/tool rhythm refactor (v0.0.97). Add `--prose-stack-sm/md/lg` (12/24/56px), `--tool-gutter` (16px), and `--measure-prose` (68ch) to `colors_and_type.css`. New `.ds-prose` editorial surface — no chrome, capped measure, asymmetric heading margins (large above, small below — headings hug their own body). `Manifesto` no longer wraps in a `Panel`/`.panel-manifesto`; it returns a bare `.ds-prose .ds-manifesto` container so editorial copy reads on bare paper. `ProjectView` lede pair (Heading + Lede) now lives in a `.ds-prose` block. `work-detail` body paragraph moved into a `.ds-prose` region so expanded works inherit the 68ch measure and rhythm. `.panel-head` padding flipped to `10px 22px 18px 22px` (more space below the heading than above — heading hugs its panel body). Panel internals migrate ad-hoc gaps to `var(--tool-gutter)` (panel-head gap, panel-body horizontal padding, ds-work-actions gap). `.ds-section:has(> .ds-prose:only-child)` drops card chrome via `:has` — prose sections render on bare paper without changing the consumer's wrapping API.
- Strip `text-transform: uppercase` from `.kpi-card .lbl` and tighten letter-spacing — the bible forbids all-caps chrome and every freddie route was shouting via the Kpi component. Lowercase the four chat composer labels in `src/components/freddie/pages-chat.js` (working directory / skill / provider / model). Remove `border-bottom` from `.fd-cfg-row` and the static 1px border on `.fd-cfg-control input/select`; replace with tonal zebra (`panel-2` on even rows) and `panel-3` control fill. Loosen `.fd-label` letter-spacing now that labels read lowercase. Witnessed across all 14 freddie dashboard routes.
- Add base padding (8px 12px), border-radius 10px, Nunito font, and tonal background to bare input elements (text/email/password/search/number) in app-shell.css to match button and textarea styling
# Changelog

## Unreleased

### Flash of Unstyled Content (FOUC) fix

- **Added render-blocking stylesheet link** to `site/theme.mjs` — `<link rel="stylesheet" href="https://unpkg.com/anentrypoint-design@latest/dist/247420.css">` placed in `<head>` before `<div id="app"></div>`. Blocks initial page paint until SDK CSS loads, eliminating visible flash when flatspace portfolio projects load anentrypoint-design from unpkg.

### Claude Code skill registration

- **Enabled `npx skills add AnEntrypoint/design -g claude-code`** — copied `SKILL.md` (324L, user-invocable) from root to `.claude/skills/design/SKILL.md` following Agent Skills GitHub discovery convention. Skill documentation includes design tokens, consuming patterns, and SDK integration examples.

### Hygiene: split deck-stage.js

- **Split `slides/deck-stage.js`** (was 227L) into `deck-stage-state.js` (66L, state/lifecycle functions) + `deck-stage.js` (159L, custom element class). Extracted: `loadNotes`, `restoreIndex`, `persistIndex`, `collectSlides`, `applyIndex` + shared constants `STORAGE_PREFIX`, `VALIDATE_ATTR`. Main class now imports and delegates to state module. Acceptance criterion met: slides/deck-stage.js ≤200L. test.js passes.

### CDN elimination per AGENTS.md portfolio rule

- **Removed Prism.js CDN loader** from `src/highlight.js`. Syntax highlighting is optional showcase feature; disabled to align with "no CDN except SDK @latest" rule.
- **Removed animate.css CDN loader** from `src/motion.js` and both UI kits (`ui_kits/homepage/app.js`, `ui_kits/project_page/app.js`). Motion system disabled as showcase enhancement, not critical feature.
- **Added `scripts/bundle-markdown.mjs`** — fetch-at-build-time pattern for marked@15 + DOMPurify@3. Bundles locally instead of loading from jsDelivr CDN at runtime.
- Lazy-loaded markdown (src/markdown.js) remains unchanged — non-blocking deferred load acceptable per rule intent.

### Re-architecture pass

- **Modularized `src/components.js`** (was 524L) → barrel re-exporting `src/components/shell.js`, `src/components/content.js`, `src/components/chat.js`. The barrel itself is 21 lines.
- **Replaced hand-rolled markdown** with `marked@15` + `DOMPurify@3` loaded as ESM from jsDelivr at first use. Block markdown (h1–h4, lists, blockquote, hr, table, code fences) now renders inside `.chat-md`. Sanitization removes `<script>`, `<iframe>`, `javascript:` URLs, `onerror`/`onclick`/etc.
- **Added Prism.js syntax highlighting** for `.chat-code` parts. Languages preloaded: markup, css, clike, javascript, jsx, tsx, typescript, json, bash, python. Tokens witnessed live for jsx (27) and python (73) blocks.
- **Added `src/bootstrap.js`** with `mountKit({ root, view, screen })` — single entry point that wires animate.css link, prism CSS link, motion shim, applyDiff render loop, and `window.__debug` registration. Replaces per-kit copy-paste boot code.
- **Added `src/debug.js`** — `window.__debug` observability registry. Subsystems register a snapshot fn; `window.__debug.list()` and `.snapshotAll()` give live state. Active subsystems: `markdown`, `highlight`, `chat`, `ds-chat`, `bootstrap`.
- **Added `<ds-chat>` web component** (`src/web-components/ds-chat.js`). Custom element with `messages` setter, attribute reflection for `title`/`sub`/`placeholder`, dispatches a bubbling/composed `send` event with `{detail:{text}}` when the user submits.
- **Split `slides/deck-stage.js`** (was 621L) into `deck-stage-style.js` (CSS template literal) + `deck-stage-overlay.js` (overlay/tapzones DOM + keyboard handler dispatch table) + `deck-stage.js` (227L, custom element class). Now an ES module — slides/index.html updated to `type="module"`.
- **Replaced inline style attribute strings** in components with `.ds-*` CSS classes. Hero, section, manifesto, work-detail, changelog, link-accent, dot-live/idle, crumb-right, pattern-notes are now class-driven.
- **Removed `vendor/rippleui-1.12.1.css`** (was 0 bytes — dead). Build manifest updated.
- **Added `test.js`** (174L, ≤200 cap) — single integration test. Boots local static server, verifies HTTP 200 on chat/aicat/slides/index. Browser group skipped if playwright not in node_modules; the witness is provided by `exec:browser` during development.
- **`npm run test`** now wired to `test.js`.

### Witnessed in browser

- chat kit: 8 messages render across 7 attachment kinds (text/code/image/pdf/file/link/md) with reactions and read-receipts; marked produced h2 + bullet list + blockquote; prism produced 27 tokens.
- aicat kit: classifier triggers code (jsx, python), md with code fence, image, pdf, file, link, text replies. python sieve highlight produces 73 tokens.
- xss vector `<script>` + `javascript:` link + `<img onerror>` all stripped — `window.__pwn` never set; no script tags inside `.chat-thread`; no surviving `javascript:` href; no `onerror` attribute.
- `<ds-chat>` web component: registers, renders 1 seeded message, fires `send` event with `'go'` when programmatically submitted.
- slides: 6 sections collected; ArrowRight advances, Home returns to 0; shadow overlay present.
- homepage kit: still renders post-refactor; h1 reads "the creative department of the internet."
